---
description: Полный цикл — аналитик → N параллельных циклов дизайнер/критик, повтор до нуля блокеров (макс. 3 итерации). Опционально — генерация pytest-тестов по завершении.
---

Вы — оркестратор многопроходной генерации e2e тест-кейсов. Следуйте этой процедуре **точно** и
не выполняйте работу агентов самостоятельно — делегируйте через инструмент `agent`.

Аргументы (могут отсутствовать): {{args}}
- путь или glob → источник требований (по умолчанию `input/requirements/*.md`)
- `--max N` → переопределить MAX_ITERATIONS (по умолчанию 3)
- `--journey J01` → выполнить цикл только для одного journey. Значение сопоставляется как
  **префикс** id без учёта регистра: `J01` находит `J01-registration-onboarding-first-play`.
  Если подошёл не ровно один journey — остановитесь и покажите список подошедших; не выбирайте
  за меня
- `--parallel N` → количество параллельных циклов дизайн/ревью; пропускает вопрос о параллельности
- `--unit journey|area` → чем владеет один цикл; пропускает вопрос о владении
- `--yes` → пропустить паузу go/no-go после анализа
- `--no-ask` → полностью автономно: пропустить все интерактивные вопросы, накопив их в
  `output/pending.md`; Фаза 4 переносит его в отчёт под «Требуются решения человека»
- `--generate-pytest` → флаг включения генерации pytest-тестов после завершения циклов
- `--selenium` → дополнительно сгенерировать браузерные тесты (`browser-test-writer`).
  Без этого флага браузерные тесты не создаются никогда
- `--base-url <адрес>` → адрес стенда для браузерных тестов. Обязателен вместе с `--selenium`,
  если адрес не зафиксирован в `input/requirements/`. Оркестратор не подставляет адрес сам

## Как задавать мне вопросы

Фазы 1.5, 2.5 и 3 обязаны остановиться и спросить меня. Это шлюзы, а не формальность.
Протокол ниже общий для всех трёх — следуйте ему дословно.

!{python3 scripts/include_skill.py human-gate --level 3}

Инструменты и рабочий каталог — если ниже названа хоть одна недостающая строка или `pwd`
не корень репозитория, остановитесь и скажите мне об этом: дальше шлюзы просто не сработают.

!{echo "pwd=$(pwd)"; for s in start_run extract_reqs build_gate_questions gate_check check_artifacts check_state review_scope validate_cases validate_plans include_skill; do [ -f "scripts/$s.py" ] || echo "НЕТ scripts/$s.py"; done; echo "проверено 10 скриптов"}

Все проверки этой процедуры — скрипты, и решение принимается по **коду выхода**, а не по тексту
вывода. Код, не названный в таблице своей фазы, считается отказом: проверка, которая не
выполнилась, — не пройденная проверка. «Скрипт не найден» и «не смог определить» останавливают
фазу ровно так же, как найденное нарушение. Иначе шлюз обходится тем, что ломается, и прогон
идёт дальше на непроверенных артефактах — то есть ровно там, где шлюз и был нужен.

Состояние проекта сейчас:

!{ls -1 input/requirements/ 2>/dev/null; echo "--- suites ---"; ls -1 output/suites/ 2>/dev/null; echo "--- cases ---"; ls -1 output/cases/ 2>/dev/null; echo "--- state ---"; ls -1 output/state/ 2>/dev/null}


## Как инструктировать агентов

Каждый вызов `agent` в Фазах 1, 2 и 2.5 собирается по протоколу ниже. Под-агенты не делят
контекст ни с вами, ни друг с другом — всё, что они знают, приходит промптом.

!{python3 scripts/include_skill.py subagent-briefing --level 3}

## Процедура

### Фаза 1 — Анализ требований

Сначала отметьте начало прогона — до вызова аналитика:

```
python3 scripts/start_run.py
```

Он убирает артефакты прошлого прогона в `output/.previous/` и записывает `output/.run`.
Без этого шага в `output/` останутся planы и кейсы journey, которых в этом корпусе уже нет,
а базы хешей `review_scope.py` от прошлого прогона будут считаться действительными.

`--keep` вместо архивации — в двух случаях, и только в них:

- вы **продолжаете прерванный** прогон;
- вы **переделываете часть готового** прогона (`--journey J02` поверх завершённого): артефакты
  остальных journey должны остаться на месте, это тот же прогон. Фаза 1 при этом всё равно
  перезапускает аналитика — если его планы изменятся для journey, которые вы не трогаете,
  остановитесь и покажите мне диф, а не молча продолжайте.

Новый корпус требований — всегда новый прогон, без `--keep`.

Затем вызовите `agent` с `subagent_type: requirements-analyst`.
Задача: прочитать источник требований, сгенерировать планы путешествий (journey) в `output/suites/`,
а также машинный индекс `output/suites/_index.json`.
Дождитесь завершения. Прочитайте `output/suites/_index.json` и каждый созданный план.
Если планов ноль — остановитесь и сообщите причину.

Покажите мне список journey: id, заголовок, приоритет, функциональные области, этапы, варианты,
пробелы, вопросы.

### Фаза 1.5 — Шлюз требований (обязательный, спросить явно)

Выполните шлюз по процедуре ниже **целиком, включая Шаг 5** — Фаза 2 запускает дизайнеров,
а Шаг 5 именно их и шлюзует. Передайте в процедуру флаги этого прогона: `--parallel`, `--unit`,
`--no-ask`.

!{python3 scripts/include_skill.py requirements-gate --level 4}

Если не передан `--yes`, сделайте паузу для моего go/no-go перед Фазой 2.

### Фаза 2 — Параллельные циклы дизайна и ревью

`P` — параллельность, выбранная мной на шлюзе; `MAX_ITERATIONS` = 3 или `--max N`;
единица владения — из `--unit`. Если задан `--journey`, список работ состоит из него одного.

!{python3 scripts/include_skill.py journey-pipeline --level 4}

### Фаза 2.5 — Генерация pytest-тестов (опционально, спрашивает человек)

Выполняется только когда Фаза 2 полностью завершена: все journey — `PASS` или `NEEDS_HUMAN`,
батч и очередь пусты. Передайте в процедуру флаги этого прогона: `--generate-pytest`,
`--selenium`, `--base-url`.

Если фаза не выполняется (ответ «нет» и флага `--generate-pytest` нет) — переходите к Фазе 3.

!{python3 scripts/include_skill.py pytest-generation --level 4}

### Фаза 3 — Шлюз покрытия (обязательный, спросить явно)

Перед написанием отчёта прочитайте каждый финальный ревью и соберите то, что прогон **не смог**
урегулировать:

1. требования в области действия journey, которые не проверяет ни один кейс — MAJOR критиков
   по покрытию,
2. уточняющие вопросы, всё ещё открытые в кейсах и планах, дедуплицированные, упорядоченные
   по количеству заблокированных кейсов,
3. пробелы, которые дизайнеры обнаружили при написании шагов, но которые аналитик не нашёл,
4. journey, завершившиеся NEEDS_HUMAN или FAILED, с их неразрешёнными блокерами дословно.

Выведите все четыре списка **полностью**, затем спросите меня через инструмент вопроса
(см. «Как задавать мне вопросы»), одним вызовом и последним действием в сообщении:

- `header`: `Итог`
- `question`: «Прогон завершён: X требований без проверки, Y открытых вопросов, Z journey требуют
  человека. Что дальше?»
- варианты: *Записать отчёт как есть (рекомендуется)* — «Фаза 4, открытые вопросы уходят в отчёт» /
  *Отвечу на вопросы, прогнать заново* — «ответы в _answers.md, повтор Фазы 2 для затронутых
  journey» / *Показать непокрытые требования* — «подробный разбор, вопрос повторяется после него».

Пустой список — это ответ, а не повод пропустить вопрос: при X=Y=Z=0 вопрос всё равно задаётся.

Если я выберу повторный прогон, добавьте мои ответы в `input/requirements/_answers.md` и
повторите Фазу 2 только для затронутых journey, сбросив их счётчики итераций.

### Фаза 4 — Отчёт

`output/report.md` создаётся **здесь и только здесь** — это единственная точка, где он пишется
за прогон. Перед записью прочитайте `output/pending.md`, если он есть: там лежат вопросы, которые
пропустили предыдущие фазы в режиме `--no-ask`, и они должны попасть в отчёт целиком, а не
исчезнуть под ним.

Напишите `output/report.md` (русский) содержащий:

- заголовок прогона: источник требований, journey, использованная параллельность, единица владения,
  итерации на каждый journey
- таблица: journey, вердикт, использованные итерации, оставшиеся блокеры, majors, minors,
  количество кейсов, количество шагов
- сводные **Выявленные пробелы** по всем journey, дедуплицированные
- сводные **Уточняющие вопросы**, нумерованные, сгруппированные по journey, упорядоченные
  по количеству блокируемых ими кейсов, каждый помечен как отвеченный / открытый
- **Непокрытые требования** — каждый `REQ-XX`, который не проверяет ни один кейс, с причиной
- **Требуются решения человека** — для `NEEDS_HUMAN`/`FAILED` journey неразрешённые блокеры
  дословно, плюс содержимое `output/pending.md` целиком: каждый вопрос, пропущенный из-за
  `--no-ask`, с его вариантами и взятым по умолчанию значением

Затем выведите краткую сводку на русском о том, что произошло, как были распределены
параллельные циклы, и что требует решения человека.
