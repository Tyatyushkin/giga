# TODO — следующая сессия

Контекст: ветка `skills`, пять полных прогонов (57:24 → 37:32 → 19:18), замеры в `result.md` § 9.
Стена времени — выходные токены; внутри агента генерация последовательна. Отсюда порядок ниже.

## O-14. JSON-двойник кейса генерировать скриптом — ПЕРВЫМ

Дизайнер пишет каждый кейс дважды (`.md` + содержательно идентичный `.json`) — это 40–45 %
его выхода, механическое преобразование по определению.

- [ ] `scripts/case_to_json.py`: парсер таблиц взять из `validate_cases.py` (`parse_table`,
      `parse_fields`, `split_sections` — уже разбирают ровно эти секции), схема —
      `docs/format.md` § 5 / `templates/test-case.json`
- [ ] Спека `qa-designer.md`: пишет только `.md`, в конце вызывает скрипт
- [ ] Паритет-проверку в линтере оставить (ремень к подтяжкам)
- [ ] Замерить на демо-корпусе Knox: ожидание 5:13 → ~3:00; на 9-кейсовом journey 27:27 → ~16

Заодно умирает класс багов «md↔json разошлись» — единственный MINOR прогона 5 был ровно им.

## O-15. Дизайнер: основной кейс → веер вариантных писателей — ВТОРЫМ

Варианты наследуют предусловия основного кейса, но не зависят друг от друга — та же изоляция,
что у journey, уровнем ниже. Конвейер: основной кейс → N параллельных писателей по варианту.

- [ ] Включать по условию **вариантов ≥ 3** (оркестратор видит таблицу «Варианты» плана);
      на 1–2 вариантах — чистый убыток от оркестрации
- [ ] Каждому писателю: план, требования, ГОТОВЫЙ основной кейс, строка границы «ты владеешь
      только TC-J01-NN»
- [ ] Проверка «варианты не пересекаются» остаётся критику (проход 4) — писатель соседей не видит
- [ ] Синергия с O-08: фикс-итерация уже знает изменённые кейсы из `review_scope` —
      исправления тоже веером, по писателю на кейс
- [ ] Ожидание на звуковском J01 (8 вариантов): 27:27 → ~10; вместе с O-14 → ~6–7

## O-16. Аналитик: архитектор → параллельные писатели планов — ТОЛЬКО ПРИ JOURNEY ≥ 2

Группировку не параллелить (journey ищутся, только глядя на весь корпус — `GIGACODE.md`);
параллелится разворачивание карты в полные планы.

- [ ] Архитектор выдаёт компактную карту (journey, этапы-скелеты, якоря REQ, пробелы — JSON)
- [ ] По писателю на journey разворачивает карту в план; `_index.json` собирает скрипт
      из фрагментов (дедуп пробелов — сборочный шаг)
- [ ] Карту валидировать (`check_state.py` расширить схемой)
- [ ] Ожидание на полном «Звуке» (3 journey): аналитик 11:07 → ~5–6

## Оговорки

- На демо-корпусе (1 journey, 2 кейса) O-15/O-16 не помогут — правит фикс-минимум четырёх
  агентов ~15 мин; там выигрывает только O-14.
- Веера предполагают реальную параллельность подагентов: на Claude измерена, на GigaCode —
  неснятый замер **O-00** (`optimize.md`). Включать O-15/O-16 на GigaCode — после него.

## Хвосты прошлых сессий (не к следующей, чтобы не потерять)

- O-10: потолок `min(3, …)` в `GIGACODE.md` — однострочник, смысл только при реальной
  параллельности (после O-00)
- O-12: MCP-сервер поверх скриптов (их уже 9)
- `validate_plans.py`: правила равномерности нарезки — кандидат из `result.md` § 8
  (journey с непропорционально большим числом вариантов = предупреждение о группировке)
- Прогон Фазы 2.5 с `--selenium` не исполнялся ни разу (нужен стенд с `--base-url`)
