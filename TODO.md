# TODO — следующая сессия

Ветки `new`, `skills` и `mcp` указывают на один коммит: 19.08.2026 в `new` сведена работа
четырёх человек (`abramov-optimized`, `snapshot`, `astrarium-optimized`).
Одиннадцать прогонов, замеры в `result.md` § 8–13.

Направление форматов с прогона 10 обратное прежнему: **JSON первичен, Markdown порождается**
из него `scripts/json_to_md.py`. Решение принято по замеру: обратное преобразование не несёт
`steps[].requirements`, прямое воспроизводит рукописный Markdown строка в строку. Цена — JSON
на 17 % длиннее.

## Прочти это до того, как оценивать оптимизации по секундомеру

Прогон 6 повторил прогон 5 **на побайтово том же входе** и занял 43:37 против 19:18 — разброс
**+126 %**, больше эффекта, который § 9 приписывал двукратному сокращению корпуса (−49 %).
Причина не в шуме: агент прогона 6 был систематически дотошнее (7 пробелов против 3, 6
блокирующих вопросов против 1), а больше найденного = больше выходных токенов = больше времени.

**Следствие для этого списка: выбирать оптимизации по механизму — что именно перестанет
генерироваться, — а не по замеру одного прогона.** Ожидания времени ниже оставлены как порядок
величины, не как обещание. Честная проверка эффекта требует n≥3 на стадию.

## O-14. JSON-двойник кейса генерировать скриптом — СДЕЛАНО (слито из abramov-optimized)

Суть пункта — «второй формат собирает скрипт, а не LLM» — закрыта. Направление в тот же день
развернули: `scripts/json_to_md.py` (Астрариум) вместо `build_case_json.py` (Абрамов), потому что
именно `steps[].requirements` и решило спор — из Markdown это поле не восстановить, а обратно
конвертер воспроизводит рукописный текст строка в строку.

`build_case_json.py` оставлен как путь миграции рукописного Markdown в JSON (см. долги слияния).
Спека `qa-designer.md` переведена: «пишу только JSON», конвертер — шаг 1 самопроверки, перезапуск
после любой правки `.json`. `check_artifacts.py` по-прежнему ждёт оба файла на кейс, и это верно:
конвертер отрабатывает внутри агента, до его возврата.

<details><summary>Исходная постановка</summary>

Дизайнер пишет каждый кейс дважды (`.md` + содержательно идентичный `.json`) — 40–45 % его
выхода, механическое преобразование по определению. **Механизм бесспорен независимо от разброса:
эти токены просто перестают генерироваться.**

- [ ] `scripts/case_to_json.py`: парсер таблиц взять из `validate_cases.py` (`parse_table`,
      `parse_fields`, `split_sections`, `normalize_cell` — уже разбирают ровно эти секции),
      схема — `docs/format.md` § 5 / `templates/test-case.json`
- [ ] Спека `qa-designer.md`: пишет только `.md`, в конце вызывает скрипт
- [ ] Паритет-проверку в линтере оставить (ремень к подтяжкам)
- [ ] `check_artifacts.py` — стадия design по-прежнему ждёт оба файла на кейс; убедиться, что
      скрипт-генератор отрабатывает до проверки полноты

Заодно умирает класс багов «md↔json разошлись» — MINOR прогона 5 был ровно им. И сбой прогона 6
пришёлся ровно на границу `.md` → `.json`: с O-14 докат сводился бы к одному варианту.

</details>

## O-15. Дизайнер: основной кейс → веер вариантных писателей — ВТОРЫМ

Варианты наследуют предусловия основного кейса, но не зависят друг от друга — та же изоляция,
что у journey, уровнем ниже.

- [ ] Включать по условию **вариантов ≥ 3** (таблица «Варианты» плана; её же читает
      `check_artifacts.py`, парсер можно переиспользовать)
- [ ] Каждому писателю: план, требования, ГОТОВЫЙ основной кейс, граница «ты владеешь только
      TC-J01-NN»
- [ ] Проверка «варианты не пересекаются» остаётся критику (проход 4) — писатель соседей не видит
- [ ] Синергия с O-08 и с доканом: и фикс-итерация, и восстановление после сбоя уже знают
      пофайловый список — `review_scope` и `check_artifacts` соответственно

## O-16. Аналитик: архитектор → параллельные писатели планов — ТОЛЬКО ПРИ JOURNEY ≥ 2

Группировку не параллелить (journey ищутся, только глядя на весь корпус — `GIGACODE.md`);
параллелится разворачивание карты в полные планы.

- [ ] Архитектор выдаёт компактную карту (journey, этапы-скелеты, якоря REQ, пробелы — JSON)
- [ ] По писателю на journey разворачивает карту в план; `_index.json` собирает скрипт из
      фрагментов (дедуп пробелов — сборочный шаг)
- [ ] Карту валидировать (`check_state.py` расширить схемой)

## Оговорки к O-15 / O-16

- На демо-корпусе (1 journey, 2 кейса) не помогут — правит фикс-минимум четырёх агентов ~15 мин;
  там выигрывает только O-14.
- Веера предполагают реальную параллельность подагентов: на Claude измерена, на GigaCode —
  неснятый замер **O-00** (`optimize.md`).

## Детерминировать то, что плавает — из прогонов 7–9

Три прогона на одном корпусе (`result.md` § 11) показали: стабильно ровно то, у чего есть внешний
ограничитель. Этапы перестали гулять, как только у них появился потолок 8. Ниже — четыре места,
где ограничителя нет, и все четыре закрываются детерминированно, без надежды на модель.

- [x] **Потолок шагов основного кейса** — `MAX_STEPS_PER_STAGE = 2` в `validate_cases.py`,
      правило `steps-too-many` (MAJOR). Порог поставлен по замеру всех основных кейсов на диске:
      1.25 / 1.38 / 1.75 (Knox) и 1.58 (эталон Звука) — двойка выше всех четырёх с запасом.
      План ищется сам (`output/suites/<JID>.md` или `<каталог кейсов>/_suite-plan.md`),
      переопределяется `--plan`; если план не найден, правило не молчит, а говорит об этом.
- [x] **Тяжесть у «утверждён код, которого нет в требованиях» зафиксирована.** Оказалось, что
      рубрика уже предписывала BLOCKER («требование молчит + кейс утверждает конкретную реакцию
      системы») и прямо запрещала понижать — прогон 9 применил её верно, прогон 7 нарушил,
      сочтя код «почти тавтологическим». Чинить надо было не тяжесть, а лазейку: в рубрике
      появился раздел «Код ответа — это реакция системы», разбирающий три довода на понижение,
      а `qa-designer.md` получил то же правило, чтобы дефект не возникал вовсе.
      Открытым осталось: **линтер этого не ловит** — сверка «код в кейсе ↔ код в REQ для той же
      операции» механическая, но требует сопоставления URL из шага с текстом требования.
- [ ] **Ссылки «пробел N ↔ вопрос N» — убрать класс, а не ловить его.** Шесть из семи MINOR
      прогона 8 были ровно этим. Дизайнер прогона 10 назвал причину точнее любой проверки:
      номера пробелов он выбирает сам, а вопросы нумеруются **позицией в массиве**, поэтому
      вставка одного вопроса молча ломает все ссылки на последующие. Дать обоим массивам явные
      `{"id": …, "text": …}` — и проверять станет нечего. Линтерная сверка остаётся запасным
      вариантом, если схему трогать не захотим.
- [ ] **Запрет плейсхолдеров `<…>` в создаваемых данных плана.** Прогоны 7–8 задали имя топологии
      в плане, прогон 9 оставил `<имя>` — и значение придумал дизайнер. При веере вариантных
      писателей (O-15) каждый придумает своё, и кейсы одного journey разъедутся. Правило для
      `validate_plans.py`: план с плейсхолдером в столбце создаваемых данных к дизайну не готов.
- [ ] **Итерация исправления не правит план.** В прогоне 9 опровергнутый `HTTP 200` убран из
      кейсов, но остался в разделе «Проверяемые состояния» плана — следующая сверка вернёт
      BLOCKER. Нужно решение: либо фикс-итерация получает право править план, либо расхождение
      «план против кейса» становится отдельной находкой критика.

## Долги первичного JSON — из прогона 10

Первый прогон, где дизайнер пишет кейс сразу в JSON. Сработало: линтер чист с первого раза,
правок после него не потребовалось. Замечания ниже — от дизайнера и критика, впервые работавших
в новом формате.

### Закрыто в тот же день

- `json_to_md.py --check` — неразрушающий ответ на вопрос «запускали ли конвертер». До него
  проверка требовала запуска, то есть самого проверяемого действия, и стирала улику. Вписан
  в самопроверку дизайнера и в детерминированный шлюз критика.
- Латентный дефект `_field`: обе ветки возвращали `fallback`, непустой список рендерился как «—».
- Устаревший `docs/format.md` § 5 и `qa-designer.md`, где `patch_case.py` был нацелен на `.md`
  (патч ушёл бы в порождаемый файл и был бы стёрт молча).
- **Паритет проверяется целиком, а не по пяти полям.** Линтер спрашивает у конвертера, каким
  Markdown обязан быть, и сравнивает файл. Раньше сверялись `id`, число шагов и три колонки —
  правка предусловий, данных, постусловий, пробелов и вопросов проходила незамеченной.
- **`|` и переводы строк в шагах разрешены**: конвертер экранирует, `parse_table` знает про
  `\|`. Запрет в § 5 снят — он держался на памяти автора.
- **`steps[].requirements` проверяется** тремя правилами: якорь существует в индексе (BLOCKER),
  шаги не выходят за «Покрываемые требования» кейса (MAJOR), каждое объявленное требование
  проверяется хотя бы одним шагом (MAJOR). Критик сверяет с полем свою карту трассируемости.
- **XML снова выразим значением**: разметка отличается от слота по закрывающему элементу.

### Осталось

- [x] **Шаг называет данные ссылкой, а не переписывает их.** Реализовано подстановкой
      `{Имя параметра}` на месте, а не списком ссылок: ячейка шага — композиция (полный URL =
      базовый адрес + путь), списком её не выразить. Проверка стала точной в обе стороны: имя
      вне таблицы — BLOCKER и остаётся неразрешённым в Markdown, строка без ссылок и без
      вхождения значения — MAJOR. Пять существующих кейсов мигрированы, порождённый Markdown
      совпал со снимком до миграции побайтово.
- [x] **Пошаговые якоря видны человеку** — конвертер порождает раздел «Трассируемость»
      (требование → шаги, прочерк у непроверенного).

### Осталось от этого раздела

Ничего. Все девять пунктов закрыты в тот же день.

<details><summary>исходный открытый пункт про видимость якорей</summary>

- **Пошаговые якоря не видны человеку.** `steps[].requirements` живёт только в JSON;
      Markdown колонки не несёт, а на шлюзе человек читает именно Markdown. Линтер и критик
      поле используют, человек — нет. Вариант: конвертер добавляет после таблицы шагов короткую
      секцию «шаг → REQ».

</details>

## Что просит писатель тестов — из прогона 11

Первый прогон, где `pytest-stub-writer` работал с первичным JSON, ссылками и «Трассируемостью».
Всё три средства он использовал и оценил как помощь; ниже — чего не хватило. Все пять про
машиночитаемость, ни одного про содержание кейсов.

- [ ] **`steps[].testData` — свободная проза.** «URL, Basic X / Y, тело запроса: Z». Метод,
      адрес, query и тело писатель разбирал из текста. Структура (`method` / `url` / `auth` /
      `body`) убрала бы догадки. Осторожно: ссылки `{Имя}` дались дизайнеру легко именно потому,
      что колонка осталась текстом — структурирование заберёт эту лёгкость, и цену надо
      взвесить, а не платить по умолчанию.
- [ ] **Нет флага «вердикт не выносится».** Где ожидаемый результат требованиями не задан,
      писатель ставит `skip` — но узнаёт об этом из формулировок («требованиями не задано»).
      `steps[].verdict: "none"` со ссылкой на пробел сделал бы генерацию детерминированной
      вместо текстовой эвристики. Заодно закрыл бы MAJOR прогона 11: кейс, чьи шаги не выносят
      вердикта, объявлял покрытие требования.
- [ ] **Ожидаемый HTTP-код только в прозе.** Отдельное поле дало бы линтеру возможность сверять
      код с требованием механически — тот самый долг «линтер этого не ловит» из раздела про
      рубрику.
- [ ] **Нумерация пробелов и вопросов локальна для кейса** и не совпадает с планом сьюты
      (пробел 3 плана = пробел 1 в `TC-J01-04`). Третий агент подряд спотыкается об это;
      решение то же — явные `id`, см. раздел про прогоны 7–9.
- [ ] **Исходное состояние системы не описано.** Ни план, ни кейсы не говорят, что лежит
      в реестре топологий до прогона — только что целевого имени там нет. Стабу пришлось
      выбрать пустой список, из-за чего проверка «каждая запись содержит `name` и `timestamp`»
      на первом шаге идёт по пустому списку. Это вопрос к шаблону плана, а не к дизайнеру.

## Долги слияния четырёх веток (19.08.2026)

- [x] **Префлайт больше не отстаёт молча.** Проверяет 13 скриптов процедуры и отдельной
      строкой называет всё, что лежит в `scripts/`, но процедурой не вызывается — теперь
      новый файл в дереве виден сразу. Переписан без опоры на разбиение слов: `for s in $req`
      в zsh не разбивается, и первая версия проверяла один «скрипт» с именем из тринадцати слов.
- [x] **`build_case_json.py` описан как путь миграции** — в докстринге сказано, что первичен
      теперь JSON, скрипт поднимает дореформенный рукописный Markdown, в конвейере не
      вызывается, и `steps[].requirements` при подъёме не восстанавливается.
- [x] **`extract_journey_context.py` подключён** — шаг 0 цепочки в `journey-pipeline`.
      Спеки дизайнера и критика обещали `<JOURNEY_ID>-context.json` и говорили «его строит
      оркестратор», а такого шага не было: четыре прогона критик читал полный индекс.
- [ ] **Фаза 2.6 (Allure TestOps) ни разу не выполнялась.** Пришла из `snapshot` с
      проработанными путями отказа, но MCP-сервера у нас нет — это первый кандидат на
      ветку `mcp`.
- [x] **`patch_case.py` подключён и проверен в бою.** Долг был записан по ошибке: секция
      «Patch, do not retype» в `qa-designer.md` появилась вместе со скриптом, я лишь
      перенацелил её с `.md` на `.json`. Дизайнер прогона 11 правил им формулировку вопроса
      и затем перегенерировал Markdown.
- [ ] **Профилировщики логов не запускались** (`profile_qwen_logs.py`, `analyze_logs_v2.py`).
      Это инструмент под долг «расход `max_turns` не мерен» — он теперь есть.

### Урок процесса: `git merge` не сигналит о тихом откате

Слияние `astrarium-optimized` бесконфликтно вернуло **шесть** наших исправлений: границы слов
в `n/a`, нормализацию в `data-unused`, саму `normalize_cell`, инвалидацию по run-id в
`review_scope`, таблицу инвалидации и правило перепечатки находок в `test-critic`, учёт
перенесённых находок в шаблоне ревью. Ветка отошла до этих правок, мы их файлы после отхода
не трогали в тех же местах — для git это чистое применение чужой версии.

Ни один не был виден в списке конфликтов. Все нашлись **прогоном инструментов**: откат `n/a`
превратил пять чистых шагов в BLOCKER на первом же запуске линтера. Отсюда правило:
после слияния ветки, отошедшей больше чем на несколько коммитов, прогонять полный набор
проверок на всех корпусах и сверять затронутые файлы с `HEAD` построчно — списка конфликтов
недостаточно.

## Долги, вскрытые прогонами 4–6

- [ ] **Дыра в демо-корпусе.** `input/requirements-knox/knox-demo.md` формулирует URL через
      `<имя>` и `<путь к файлу>`, поэтому REQ-05 требует сверки «байт в байт» с эталоном,
      которого корпус не задаёт. Поймал один аналитик из двух — прогон 6 объявил пробелом,
      прогон 5 прошёл мимо. Задать конкретные значения стенда в требованиях.
- [ ] **Расход `max_turns` не мерен.** Стоит 40 у четырёх агентов и 30 у критика, поставлено
      вслепую. Померить фактическое число ходов, прежде чем считать лимит достаточным —
      упёршийся в лимит агент возвращается как обычный, просто с меньшим числом файлов
      (это и ловит `check_artifacts.py`, но лучше не доводить).
- [ ] **Правила линтера проверять на текстах, которые они увидят.** Три ложных блокера подряд
      одного класса: `<` в арифметике, `n/a` внутри `admi·n/a·pi`, `data-unused` по токену
      в бэктиках. Пройтись по остальным правилам с этим вопросом.
- [ ] **Зависание агента** — единственный режим отказа без автоматики: оркестратор не управляет
      таймаутом вызова. Пока документировано как работа человека (`journey-pipeline`,
      «Сбои и докат»).

## Сделано после прошлого TODO — не переоткрывать

- `check_artifacts.py` + политика доката вместо безусловного `FAILED` (`journey-pipeline`,
  «Сбои и докат»; промпт доката — в `subagent-briefing`)
- `sync_markers.py` удалён; маркеры `J<NN>` регистрирует корневой `conftest.py` по каталогам
  `output/tests/` — `pytest.ini` больше не мутирует конвейером
- `data-unused` исправлен (сравнение по нормализованному тексту)
- `result.md` § 10 — переоценка надёжности всех прошлых замеров
- `review_scope.py` хеширует корпус этого прогона (`requirementsSource` из индекса
  аналитика), а не зашитый `input/requirements` — fail-open найден критиком прогона 9
  и подтверждён замером с контролем (`b255f73`)
- `extract_reqs.py --answers` вместо второго индексатора `merge_reqs_with_answers.py`;
  ответы человека получают своё пространство id `ANS-NN` (`e8ef8a8`)

## Хвосты прошлых сессий

- O-10: потолок `min(3, …)` в `GIGACODE.md` — однострочник, смысл только при реальной
  параллельности (после O-00)
- O-12: MCP-сервер поверх скриптов (их уже 9) — ради него заведена ветка `mcp`
- `validate_plans.py`: правила равномерности нарезки — кандидат из `result.md` § 8
- Прогон Фазы 2.5 с `--selenium` не исполнялся ни разу (нужен стенд с `--base-url`)
