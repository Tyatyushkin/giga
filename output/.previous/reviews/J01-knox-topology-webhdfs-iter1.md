# Отчёт критика — J01-knox-topology-webhdfs, итерация 1

## Сводка

| Показатель | Значение |
|---|---|
| Journey | J01-knox-topology-webhdfs |
| Итерация | 1 |
| Кейсов в journey | 4 |
| Переревьюено на этой итерации | 4 |
| Findings перенесено с итерации 0 | 0 |
| BLOCKER | 0 |
| MAJOR | 1 |
| MINOR | 7 |

`BLOCKER` / `MAJOR` / `MINOR` считают все находки journey, включая перенесённые: это те же числа,
что уходят в `output/state/J01-knox-topology-webhdfs.json`.

## Результат линтера

`scripts/validate_cases.py output/cases/J01-knox-topology-webhdfs` — exit code 0, findings: 0
(BLOCKER 0, MAJOR 0, MINOR 0). Проверено 4 файла кейсов. Отчёт:
`output/reviews/J01-knox-topology-webhdfs-lint.json`. Линтер прошёл по всем кейсам.

Дополнительно проведена машинная сверка Markdown ↔ JSON по всем полям (`Общая информация`,
предусловия, таблица данных, шаги — действие / данные / ожидаемый результат, постусловия, пробелы,
вопросы): расхождений нет ни в одном из 4 кейсов. BLOCKER-6 («Markdown и JSON разошлись») снят.

## Объём этой итерации

Из `scripts/review_scope.py` — «полное ревью: базы нет — первая итерация или база нечитаема»
(`savedShare` 0.0).

| Кейс | Объём |
|---|---|
| TC-J01-00 | переревьюен — полное ревью: базы нет, первая итерация |
| TC-J01-01 | переревьюен — полное ревью: базы нет, первая итерация |
| TC-J01-02 | переревьюен — полное ревью: базы нет, первая итерация |
| TC-J01-03 | переревьюен — полное ревью: базы нет, первая итерация |

## Трассируемость

Прямой проход: каждый ожидаемый результат → требование. Обратный проход: каждый REQ в области
плана сьюты → шаг, который его проверяет.

| REQ | Проверяется в | Статус |
|---|---|---|
| REQ-01 | TC-J01-00, шаги 1–10 (верные учётные данные, запрос передан сервису); TC-J01-01, шаг 2 и TC-J01-02, шаг 1 (HTTP 401 + текст `Authentication failed`) | покрыто, обе ветки |
| REQ-02 | TC-J01-00, шаг 1 (имени нет до деплоя), шаг 2 (`PUT` → HTTP 200), шаг 3 (появление записи с `name`/`timestamp` в окне 30 с); TC-J01-01, шаг 3; TC-J01-02, шаг 2 | покрыто |
| REQ-03 | TC-J01-00, шаг 4; TC-J01-02, шаг 3; TC-J01-03, шаги 1–2 | покрыто |
| REQ-04 | TC-J01-00, шаг 5 (HTTP 201, заголовок `Location` с путём `/tmp/demo-e2e.txt`) | покрыто |
| REQ-05 | TC-J01-00, шаги 6–7 (HTTP 200 + побайтовое сравнение, 27 байт) | покрыто |
| REQ-06 | TC-J01-00, шаг 8 (`DELETE` → HTTP 200), шаг 9 (записи нет в реестре), шаг 10 (HTTP 404 на gateway-URL) | покрыто |

Непокрытых требований в области journey нет: план сьюты заявляет REQ-01…REQ-06, все шесть
проверяются наблюдаемыми шагами.

Ожидаемые результаты без требования:

| Кейс, шаг | Утверждение | Есть в пробелах? | Вывод |
|---|---|---|---|
| TC-J01-00, шаги 1, 3, 9 | HTTP-код ответа реестра не проверяется, наблюдается только тело | да — пробел 1, вопрос 2 | допустимо: неопределённость помечена честно, шаг проверяет только описанное в REQ-02 |
| TC-J01-00, таблица данных | хост `https://knox-demo.stand.local:8443`, путь `/tmp/demo-e2e.txt` | да — пробел 8, вопрос 1 | допустимо: это тестовые данные прогона, а не ожидаемый результат; плейсхолдер здесь запрещён форматом |
| TC-J01-01, таблица данных | неверный пароль `admin-password-wrong` | да — пробел 9 (новый), вопрос 2 кейса | допустимо |
| TC-J01-01, шаг 3 | после HTTP 401 записи `demo-e2e` в реестре не появилось | требование есть: REQ-01 «Запрос с верными учётными данными передаётся сервису» + REQ-02 (деплой возможен только через `PUT` от `admin`) | допустимо, выводится из текста |
| TC-J01-02, шаг 2 | отклонённый по REQ-01 запрос не снял топологию | да — пробел 10 (новый) | допустимо |
| TC-J01-03, шаг 2 | результат REQ-03 одинаков для `guest` и `admin` — сравниваются только код и наличие поля | да — пробел 11 (новый) | допустимо по трассируемости; избыточность шага — MINOR #6 |
| TC-J01-00, шаг 3; TC-J01-02, шаг 2 | «непустым полем `timestamp`» | нет | MINOR #7: усиление REQ-02, который требует лишь наличия поля |

Выдуманного поведения (BLOCKER-1) не найдено. Значения, которые корпус не определяет — адрес
стенда, HTTP-код `GET …/topologies`, реакция на повторный деплой, судьба файла после снятия
топологии — во всех четырёх кейсах вынесены в «Выявленные пробелы» и «Уточняющие вопросы», а шаги
проверяют только то, что описано требованиями. Это правильное поведение дизайнера в режиме
`--no-ask`, а не дефект.

## Находки

```
#1 [MAJOR] TC-J01-00, шаги 4–5 — созданное состояние не потребляется
Что не так: ожидаемый результат шага 4 объявляет «Тело сохранено как снимок каталога `/tmp` до
записи файла прогона», но ни один последующий шаг этот снимок не использует. Шаг 5 проверяет
только HTTP 201 и заголовок `Location`, повторного листинга после записи нет.
Почему: план сьюты (`output/suites/J01-knox-topology-webhdfs.md`, этап 5) объявляет входным
состоянием этапа 5 «файла `/tmp/demo-e2e.txt` нет в снимке этапа 4» — кейс это состояние не
наблюдает. По критерию 2 шаг создаёт значение, которое никто не потребляет; по критерию 7
изменение состояния HDFS (появление файла в каталоге) подразумевается, а не проверяется.
Требованиями это не запрещено: REQ-04 задаёт наблюдаемыми только HTTP 201 и `Location`.
Исправить: в ожидаемом результате шага 4 явно проверить, что в отданном `FileStatuses` нет записи
с именем `demo-e2e.txt` (это и есть снимок «до»), и сослаться на этот снимок в шаге 5 как на
потребляемое состояние. Если расширять наблюдаемые не хотите — убрать из шага 4 фразу про
сохранённый снимок, чтобы кейс не объявлял данные, которых не использует.
```

```
#2 [MINOR] TC-J01-00, «Постусловия» — ссылка на несуществующий вопрос
Что не так: два постусловия ссылаются на «вопрос 7» («…требованиями не определено (пробел 7,
вопрос 7)» и «…до ответа на вопрос 7 повторный прогон стартует с уже существующим файлом»), но в
разделе «Уточняющие вопросы» этого кейса шесть вопросов. Судьбе файла соответствует вопрос 4.
Почему: номера пробелов кейс наследует у плана сьюты (сквозная нумерация 1…11), а вопросы
перенумерованы локально — ссылки «вопрос N» остались от плана. Читатель кейса не может разрешить
ссылку.
Исправить: заменить оба вхождения «вопрос 7» на «вопрос 4».
```

```
#3 [MINOR] TC-J01-01 — три ссылки на несуществующие вопросы и пробел вне раздела
Что не так:
  • таблица данных, строка «URL реестра топологий»: «(пробел 8, вопрос 1)» — вопрос об адресе
    стенда в этом кейсе идёт под номером 4, а вопрос 1 — про заголовок `Authorization`;
  • шаг 1, ожидаемый результат: «(пробел 1, вопрос 2)» — вопрос про HTTP-код реестра в этом
    кейсе идёт под номером 3, а вопрос 2 — про пустой пароль;
  • таблица данных, строка «Неверный пароль»: «(вопрос 5)» — вопросов в кейсе четыре, нужному
    соответствует номер 2;
  • «пробел 8» процитирован дважды (таблица данных и вопрос 4), но в разделе «Выявленные пробелы»
    этого кейса его нет — там только пробелы 4, 1 и 9.
Почему: та же рассинхронизация локальной нумерации вопросов с нумерацией плана. Пробел 8
оправдывает подстановку `https://knox-demo.stand.local:8443`, поэтому в кейсе, который эту
подстановку использует, он должен быть объявлен.
Исправить: «вопрос 1» → «вопрос 4», «вопрос 2» → «вопрос 3», «вопрос 5» → «вопрос 2»; добавить
пробел 8 в раздел «Выявленные пробелы» (формулировка есть в TC-J01-00 и TC-J01-03).
```

```
#4 [MINOR] TC-J01-02 — ссылка на несуществующий вопрос и пробел вне раздела
Что не так: таблица данных, строка «URL листинга каталога в HDFS»: «(пробел 8, вопрос 1)». Вопрос
об адресе стенда в этом кейсе идёт под номером 4, вопрос 1 — про заголовок `Authorization`. Пробел
8 при этом в разделе «Выявленные пробелы» кейса отсутствует (там пробелы 4, 1 и 10), хотя вопрос 4
на него ссылается. Ссылка «(пробел 1, вопрос 2)» в шаге 2 верна — её трогать не нужно.
Исправить: «вопрос 1» → «вопрос 4»; добавить пробел 8 в раздел «Выявленные пробелы».
```

```
#5 [MINOR] TC-J01-03, таблица данных — ссылка на несуществующий вопрос
Что не так: строка «URL листинга каталога в HDFS» ссылается на «(пробел 8, вопрос 1)», но вопрос 1
этого кейса — про админский API от `guest`; адресу стенда соответствует вопрос 4. Сам пробел 8 в
разделе пробелов присутствует — расходится только номер вопроса.
Исправить: «вопрос 1» → «вопрос 4».
```

```
#6 [MINOR] TC-J01-03, шаг 2 — контрольный шаг не добавляет наблюдаемого
Что не так: шаг 2 повторяет `GET …/gateway/demo-e2e/webhdfs/v1/tmp?op=LISTSTATUS` от `admin` и
ожидает HTTP 200 с полем `FileStatuses` — дословно то же, что уже проверяют TC-J01-00, шаг 4 и
TC-J01-02, шаг 3. Предусловия варианта уже утверждают, что gateway-URL обслуживает запросы
REQ-03…REQ-05, то есть шаг 2 подтверждает унаследованное предусловие.
Почему: заявленный вывод шага — «результат операции REQ-03 одинаков для обеих учётных записей» —
собственный пробел 11 кейса ослабляет до сравнения кода и наличия поля, а содержимое `FileStatuses`
не сравнивается. Информации сверх TC-J01-00, шаг 4 шаг не даёт. Покрытия это не теряет (REQ-03 и
без него проверен шагом 1), поэтому не MAJOR.
Исправить: либо убрать шаг 2 и оставить вариант однoшаговым отклонением «та же операция от
`guest`», либо — после ответа на вопрос 2 кейса — усилить его до сравнения тел ответов `guest` и
`admin`, что и сделает контроль осмысленным.
```

```
#7 [MINOR] TC-J01-00, шаг 3 и TC-J01-02, шаг 2 — ожидаемый результат строже требования
Что не так: оба шага требуют запись «с `name` = `demo-e2e` и непустым полем `timestamp`». REQ-02
говорит только «каждая запись содержит поля `name` и `timestamp`» — непустоту значения корпус не
определяет. Те же формулировки повторены в постусловиях TC-J01-02 и TC-J01-03.
Почему: это не выдуманное поведение (BLOCKER-1), а ужесточение существующего наблюдаемого: ответ,
удовлетворяющий REQ-02 с пустым `timestamp`, провалит шаг. Поэтому MINOR, а не BLOCKER.
Исправить: либо ослабить до «запись содержит поля `name` и `timestamp`» по букве REQ-02, либо
оставить проверку непустоты и добавить её как пробел с вопросом «гарантирует ли шлюз непустое
значение `timestamp`».
```

```
#8 [MINOR] output/suites/J01-knox-topology-webhdfs.md, «Проверяемые состояния» — заявлен
переход, который ни один этап не наблюдает
Что не так: строка «Доступность gateway-URL топологии» объявляет переход «URL не обслуживает →
обслуживает REQ-03…REQ-05 → отвечает HTTP 404». Первое звено не наблюдает ни один этап плана и ни
один шаг TC-J01-00: до шага 2 запросов к `…/gateway/demo-e2e/…` не отправляется.
Почему: наблюдать его и нельзя — корпус не определяет, что возвращает gateway-URL не развёрнутой
топологии (это соседний случай пробела 5). Кейс поступает правильно, не проверяя неопределённое;
переобещает именно таблица состояний плана.
Исправить: в плане сократить переход до «обслуживает REQ-03…REQ-05 → отвечает HTTP 404» или
пометить первое звено как непроверяемое со ссылкой на пробел 5. Кейсы менять не нужно.
```

## Оценка по критериям качества

| # | Критерий | Оценка | Обоснование (файл, шаг) |
|---:|---|---|---|
| 1 | Полнота сквозного сценария | OK | TC-J01-00: 10 шагов, все 8 этапов плана отображены (этап 6 → шаги 6–7, этап 8 → шаги 9–10). Шаг 1 — вход в путь (снимок реестра до деплоя), шаг 10 — цель journey (HTTP 404 на gateway-URL снятой топологии). Порог 8 шагов пройден |
| 2 | Логическая связность | MAJOR | TC-J01-00: имя `demo-e2e`, gateway-URL, путь `/tmp/demo-e2e.txt` и содержимое `knox-e2e-payload-2026-08-19` переносятся из шага в шаг дословно, шаги перетасовать нельзя. Единственный разрыв — снимок `/tmp` шага 4, который никто не потребляет (находка #1) |
| 3 | Корректность предусловий | OK | TC-J01-00, «Предусловия»: LDAP с двумя УЗ, порт 8443, доступный WEBHDFS, подготовленный `demo-e2e.xml`, отсутствие топологии — шаг 1 исполним без домыслов. Варианты наследуют явно и называют точку ветвления: TC-J01-01 — шаг 2, TC-J01-02 и TC-J01-03 — шаг 4, с перечислением выполненных шагов 1–3 |
| 4 | Качество тестовых данных | OK | TC-J01-00: 11 строк, каждое значение использовано хотя бы одним шагом (проверено поимённо); плейсхолдеров нет. Подставленные прогоном значения (`knox-demo.stand.local:8443`, `/tmp/demo-e2e.txt`, `admin-password-wrong`) помечены в столбце «Комментарий» ссылкой на пробел |
| 5 | Атомарность шагов | OK | Один запрос — один шаг во всех кейсах; TC-J01-00 отделяет чтение (шаг 6) от побайтового сравнения (шаг 7), а не сливает их |
| 6 | Проверяемость результатов | OK | Наблюдаемые везде машинные: HTTP-код, поле JSON (`FileStatuses`, `name`, `timestamp`), заголовок `Location`, текст `Authentication failed`, длина 27 байт. Запрещённых формулировок из `docs/format.md` в ожидаемых результатах нет (проверено грепом; единственное «работает» — в поле «Цель» TC-J01-00, где это описание деятельности, а не результат шага) |
| 7 | Покрытие состояний | MAJOR | Реестр топологий (нет → есть → нет: шаги 1, 3, 9), доступность gateway-URL (обслуживает → 404: шаги 4, 10), окно 30 с (шаг 3), целостность содержимого (шаги 6–7) проверяются переходами. Не проверяется появление файла в каталоге `/tmp` — находка #1 |
| 8 | Работа с неоднозначностями | OK | TC-J01-00 — 6 пробелов, TC-J01-01/02/03 — по 3, включая новые 9, 10 и 11, найденные сверх плана сьюты. Все блокирующие неопределённости корпуса (адрес стенда, код `GET …/topologies`, повторный деплой, судьба файла) объявлены, а не домыслены |
| 9 | Качество уточняющих вопросов | OK | Каждый вопрос называет шаг, который он блокирует, и ответ на него меняет шаг: напр. TC-J01-00, вопрос 2 → шаги 1, 3, 9 получат проверку HTTP-кода; TC-J01-03, вопрос 2 → шаг 2 усилится до сравнения тел. Номера ссылок при этом сбиты — MINOR #2–#5 |
| 10 | Отсутствие выдуманных функций | OK | Все ожидаемые результаты трассируются до REQ-01…REQ-06 (таблица выше). Единственное отклонение — усиление «непустым `timestamp`» (MINOR #7), не создающее нового поведения |
| 11 | Переиспользуемость | OK | Кейсы следуют структуре `docs/format.md` без изменений заголовков; доменная специфика Knox лежит только в содержимом полей, не в структуре |
| 12 | Структурированность | OK | `scripts/validate_cases.py` — exit 0, 0 находок на 4 файлах. Машинная сверка MD ↔ JSON: полное совпадение по всем полям во всех кейсах |

## Что делать дальше

- Кому: qa-designer
- Обязательно к исправлению: BLOCKER — нет. Новая итерация дизайнера по блокерам не требуется.
- Желательно: MAJOR #1 (снимок `/tmp` шага 4 — либо проверить отсутствие `demo-e2e.txt` в
  листинге, либо убрать объявление снимка).
- Косметика одной правкой: MINOR #2–#5 — сквозная сверка ссылок «вопрос N» с локальной нумерацией
  разделов «Уточняющие вопросы» и добавление пробела 8 в TC-J01-01 и TC-J01-02.
- MINOR #6 и #7 — по решению дизайнера; #8 адресован плану сьюты, а не кейсам.
- Человеку: 10 открытых вопросов корпуса (см. `openQuestions` в состоянии). Без ответов на вопросы
  об адресе стенда и HTTP-коде `GET …/topologies` кейсы исполнимы, но часть наблюдаемых остаётся
  непроверенной по честно объявленной причине.

ВЕРДИКТ: PASS
