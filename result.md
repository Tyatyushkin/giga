# Результаты замеров — база «до»

**Ветка:** `optimized` (`2b721f2`)
**Дата:** 29.07.2026
**План:** `optimize.md`
**Метод:** детерминированные измерения без прогона агентов

---

## Оговорка о переносимости

| Тип измерения | Переносится на GigaCode / DeepSeek-Qwen |
|---|---|
| Поведение Python-скриптов | ✅ **полностью** — тот же код, тот же результат |
| Объём входа в байтах | ✅ полностью |
| Объём входа в токенах | ❌ другой токенизатор |
| Время прогона, скорость генерации | ❌ другая модель и инфраструктура |
| Число обращений к файлам у агента | ⚠️ частично — зависит от модели |

**Всё в разделе 1 — из первых двух строк таблицы.** Это настоящая база «до», её можно
сравнивать после изменений на любой модели.

Раздел 2 — то, что требует прогона агентов и переносится лишь частично.

---

## 1. Детерминированные измерения

### 1.1. O-05 — ложные блокеры линтера на символе `<`

Проверка `has_placeholder()` из `scripts/validate_cases.py` на реалистичных формулировках:

| Формулировка | Тип | Заблокировано | Верно |
|---|---|---|---|
| `время отклика < 2 сек` | легитимная | ✅ да | ❌ **ложный блокер** |
| `счётчик треков < 100` | легитимная | ✅ да | ❌ **ложный блокер** |
| `длина названия <= 100 символов` | легитимная | ✅ да | ❌ **ложный блокер** |
| `позиция в очереди < позиции текущего` | легитимная | ✅ да | ❌ **ложный блокер** |
| `таймер обратного отсчёта < 60 с` | легитимная | ✅ да | ❌ **ложный блокер** |
| `<название плейлиста>` | заглушка | ✅ да | ✅ верно |
| `<любой трек>` | заглушка | ✅ да | ✅ верно |
| `Плеер показывает обложку и таймлайн` | легитимная | ❌ нет | ✅ верно |

```
верно: 3/8      ложных блокеров: 5/5 (все формулировки со сравнением)
```

**Каждая формулировка со сравнением блокируется.** В связке с `test-critic.md:50` («никогда не
противоречить линтеру по структурным фактам») это лишняя итерация дизайнер+критик всякий раз,
когда кейс проверяет длительность, лимит, счётчик или порядок.

**Критерий «после»:** первые пять — не блокируются, последние три — как есть.

### 1.2. O-04 — валидация `state/<J>.json`

Симуляция того, что делает `run.md:198` (`blockers == 0`) с невалидным вердиктом:

| Что записал агент | `blockers` после парсинга | `blockers == 0` | JSON валиден |
|---|---|---|---|
| `{"blockers": 0}` — корректно | `0` | `True` → PASS | да |
| `{"blockers": "0"}` — строка | `"0"` | **`False`** | да |
| поле пропущено | `None` | **`False`** | да |
| `{"state": {"blockers": 0}}` — лишний уровень | `None` | **`False`** | да |

**Гипотеза не подтвердилась — режим отказа другой и хуже, чем я предполагал.** Раньше я
писал, что битый state может дать ложный `PASS`. Измерение показывает обратное: во всех трёх
случаях сравнение даёт `False`, journey **остаётся активным** и уходит на следующую итерацию.

Фактический сценарий: journey, который на самом деле прошёл, сжигает все три итерации
дизайнер+критик и получает вердикт `NEEDS_HUMAN`. Молча — `run.md:200` ловит только «нет файла»,
а файл есть и JSON валиден.

**Цена одного такого случая: 3 лишние итерации на journey.** Это делает O-04 не только вопросом
качества, но и одним из самых дорогих мест по времени.

**Критерий «после»:** `check_state.py` возвращает ненулевой код на строках 2–4 таблицы.

### 1.3. O-13 — объём входа аналитика

| Файл | Байт |
|---|---|
| `input/requirements/zvuk.md` | 23 278 |
| `GIGACODE.md` | 17 628 |
| `docs/format.md` | 5 500 |
| `input/requirements/_answers.md` | 4 166 |
| `input/requirements/zvuk-sample.md` | 3 819 |
| `templates/suite-plan.md` | 1 975 |
| **Итого** | **56 366 байт, 6 файлов** |

`max_turns` аналитика: **25**.

Отдельно: `GIGACODE.md` — 17,6 КБ, второй по величине вход, и он загружается в каждую сессию
**дополнительно** к чтению аналитиком. Это аргумент в пользу O-11 (факторинг), который я
недооценил: там речь шла про `run.md`, но `GIGACODE.md` крупнее.

**Критерий «после»:**
- число файлов, которые аналитик читает раздельно → 1–2 вместо 6
- после O-03 — `_reqindex.json` вместо разбора 23 КБ прозы
- после снятия `zvuk-sample.md` с glob → минус 3 819 байт и минус сверка двух нумераций

### 1.4. Коды возврата шлюзов на пустом состоянии

| Скрипт | Код | Ожидание |
|---|---|---|
| `build_gate_questions.py` (нет `_index.json`) | 2 | ✅ по докстрингу |
| `gate_check.py` (нет payload) | 2 | ✅ по докстрингу |
| `validate_cases.py` (нет `output/cases`) | 2 | ✅ по докстрингу |
| `validate_cases.py` на `examples/J01-…` | 0 | ✅ BLOCKER 0, MAJOR 0, MINOR 0 |

Эталонный кейс проходит линтер чисто — это рабочий smoke-тест, его результат не должен
измениться ни от одной оптимизации.

---

## 2. Фаза 1 — фактический прогон аналитика

**Выполнен 29.07.2026, 13:35:35 → 13:48:43.** Корпус: `zvuk-sample.md` + `_answers.md`
(7 985 байт). `zvuk.md` исключён намеренно — иначе измерялась бы стоимость дефекта T-07
(сверка двух схем нумерации), а не работа пайплайна.

### 2.1. Итог

| Метрика | Значение |
|---|---|
| Время | **12 мин 15 с** |
| Токенов | 84 740 |
| Вызовов инструментов | 15 |
| Journey создано | 3 (7, 11 и 8 этапов; 6, 5 и 6 областей) |
| Структурных предупреждений | 0 — все journey прошли пороги ≥5 этапов / ≥3 области |
| Пробелов найдено | 20 |
| Вопросов поднято | 22 |
| Непокрытых REQ | 1 (`REQ-11`, с причиной) |

Индекс валиден, все обязательные ключи на месте. Два жёстких противоречия найдены корректно
(REQ-05 против ответа Q-02, REQ-12 против ответа Q-01) — механизм выявления пробелов работает.

### 2.2. Объём: вход 8 КБ → выход 89 КБ

| Что | Байт |
|---|---|
| Вход (требования) | 7 985 |
| Планы journey (3 файла, 368 строк) | 62 830 |
| `_index.json` | 26 089 |
| **Выход всего** | **88 919** |

**Одиннадцатикратное расширение.** Это подтверждает исходный тезис: время аналитика уходит в
печать, а не в чтение. 89 КБ выхода за 12 минут — порядка 120 байт/с последовательной генерации.

### 2.3. Доля транскрипции — гипотеза не подтвердилась

| Где | Байт | Доля выхода |
|---|---|---|
| Разделы «Индекс требований» в 3 планах | 8 027 | 12 % |
| `reqIndex` в `_index.json` (17 требований) | 5 738 | 21 % от индекса |
| **Транскрипция суммарно** | **13 765** | **15 %** |

**Я оценивал эту долю выше.** В `optimize.md` сказано «текст требований перепечатывается до
четырёх раз» — фактически это 15 % выхода, а не основная его часть.

**Что это меняет:**

- **O-03 по скорости — умеренный выигрыш**, около 15 % печати Фазы 1, а не кратный. Его ценность
  смещается в сторону экономии *изучения* (не искать требования в прозе) и надёжности
  (детерминированный индекс вместо пересказа), а не экономии печати.
- **O-07 (декомпозиция) — вот где основной резерв.** Остальные 85 % выхода — это содержимое
  планов, по ~20,5 КБ на journey, и оно пишется **последовательно**. Три плана параллельно
  вместо трёх подряд — это и есть кратное ускорение Фазы 1, если параллельность реальна (O-00).

### 2.4. O-13 — чтение: замер и важная оговорка

| Метрика | Значение |
|---|---|
| Файлов прочитано отдельными вызовами | 7 |
| Всего вызовов чтения | 9 |
| Пакетное чтение нескольких файлов одним вызовом | **нет** |

Но: агент **сам** сгруппировал вызовы в параллельные пачки внутри одного сообщения (4 чтения в
одном сообщении, 2 в другом), то есть число round trip меньше девяти.

**Оговорка о переносимости — существенная.** Параллельная группировка вызовов внутри сообщения —
это поведение Claude по умолчанию. Будут ли DeepSeek/Qwen делать так же без указания, неизвестно;
скорее нет. **Значит на вашей инфраструктуре O-13 может дать больше, чем показывает этот замер, а
не меньше.** Здесь измерена почти лучшая из возможных картин.

Отдельно: `read_many_files` в этом харнессе недоступен — в GigaCode он у аналитика есть
(`requirements-analyst.md:6`), так что там доступен и настоящий пакетный вызов.

### 2.5. Побочная находка — расхождение ответов с корпусом

Ответ Q-04 в `_answers.md` ссылается на журни из `zvuk.md` («J01/J02 — существующие аккаунты»),
которого в этом прогоне нет. Аналитик не стал подгонять нумерацию под недоступный документ, а
записал расхождение как пробел G-19 и вопросы Q-23/Q-27.

Это ровно то поведение, ради которого проект построен — молчание источника не заполняется
правдоподобной догадкой. И одновременно это подтверждение T-07: `_answers.md` сейчас смешивает
две схемы требований.

---

## 3. Фаза 2 — волна дизайнеров

Три дизайнера запущены **одним сообщением**, параллельно, как требует `run.md:193`.

### 3.1. Результат

| Journey | Время | Кейсов | Шагов в основном | Токенов | Линтер |
|---|---|---|---|---|---|
| J03-free-playback-resilience | 16 мин 37 с | 6 | 21 | 93 995 | 0/0/0 ✅ |
| J01-registration-and-first-play | 43 мин 36 с | 6 | 16 | 99 658 | 0/0/0 ✅ |
| J02-collection-and-session-restore | 48 мин 54 с | 7 | 25 | 110 476 | 0/0/0 ✅ |

**Итого: 19 кейсов Markdown + 19 JSON, линтер по всему каталогу — BLOCKER 0, MAJOR 0, MINOR 0.**
Все три самоотчёта проверены независимым прогоном линтера и совпали.

Суммарно за прогон: **388 869 токенов** (аналитик 84 740 + дизайнеры 304 129).

### 3.2. O-06 — цена барьера измерена

```
разброс времён дизайнеров: 2.94x   (16:37 против 48:54)
```

`run.md:189-204` описывает lockstep-волну: критики не стартуют, пока не закончат **все**
дизайнеры. Отсюда простой:

| Кто ждёт | Сколько |
|---|---|
| критик J03 | **32 мин 16 с** |
| критик J01 | 5 мин 17 с |
| **суммарно впустую** | **37 мин 33 с** |

**Это на одной итерации при трёх journey.** При `MAX_ITERATIONS = 3` барьер способен стоить
около двух часов чистого простоя.

Раньше O-06 обосновывался формулой `max(дизайнеры) + max(критики)` против
`max(дизайнер + критик)`. Теперь есть замер, и он крупнее ожидаемого: **разброс почти
трёхкратный**, а journey изолированы по построению — барьер не даёт взамен ничего.

**O-06 переходит из «зависит от результата O-00» в приоритетные.** Оговорка сохраняется: если
на вашей инфраструктуре агенты встают в очередь на один эндпоинт, выигрыш будет меньше — но
разброс никуда не денется, он свойство задач, а не железа.

### 3.3. O-05 — ложный блокер не сработал ни разу

Проверено: в 19 кейсах **нет ни одного символа `<`**. Дизайнеры выразили все лимиты словами —
«лимит пропусков», «не менее 3 жанров», «через 60 секунд».

**Это снижает оценку O-05.** В `optimize.md` он назван «гарантированной лишней итерацией» —
фактически это **латентная ловушка, а не налог на каждый прогон**. Сам баг реален (прямая
проверка функции: 5 ложных срабатываний из 5), но срабатывает лишь когда дизайнер выбирает
операторную запись.

**Открытый вопрос, не проверяемый по артефактам.** Дизайнеры по инструкции сами прогоняли линтер
перед сдачей. Возможно, они переписали формулировки, которые он забраковал — тогда линтер молча
формирует стиль выхода, вынуждая избегать законных записей вида «< 60 с». Финальные файлы этого
не показывают: в них видно результат, а не путь к нему.

### 3.4. Качество выхода — механизм работает

Ни один дизайнер не выдумал поведение. Все трое, столкнувшись с неопределённостью, записали
наблюдаемый факт и явно отложили вердикт до блокирующего вопроса:

- J02 в `TC-J02-02` (название 101 символ) фиксирует счётчик символов в поле, но не утверждает
  реакцию системы — REQ-08 её не определяет
- J03 в `TC-J03-01` записывает состояние устройства после обрыва, но не исход переподключения
- J01 в `TC-J01-05` явно помечает, что «Порядок очереди сохраняется» допускает два прочтения,
  и указывает, какое принято

Дизайнеры нашли **19 новых пробелов** сверх 20, найденных аналитиком, — те, что видны только при
написании конкретных шагов.

---

## 4. Фаза 2 — волна критиков

Три критика запущены одним сообщением, по одному на journey.

### 4.1. Результат

| Journey | Время | BLOCKER | MAJOR | MINOR | Вердикт |
|---|---|---|---|---|---|
| J01-registration-and-first-play | 7 мин 43 с | 0 | 2 | 7 | **PASS** |
| J03-free-playback-resilience | 10 мин 04 с | 1 | 7 | 7 | FIX_REQUIRED |
| J02-collection-and-session-restore | 11 мин 51 с | 1 | 4 | 8 | FIX_REQUIRED |

Критики вчетверо-впятеро быстрее дизайнеров: 7–12 минут против 16–49.

**Цикл не сошёлся за одну итерацию.** Один journey из трёх закрылся, два уходят на итерацию 2.

### 4.2. Семантический слой ловит то, чего скрипт не видит — подтверждено дважды

Линтер по всем 19 кейсам дал **0/0/0**, дизайнеры прогоняли его сами перед сдачей и считали
работу чистой. Критики нашли **2 BLOCKER, 13 MAJOR, 22 MINOR**.

**Блокер J03 — утверждение без наблюдаемого:**

```
| 10 | Наблюдать поведение приложения при отключённой сети | — |
     Приложение выполняет попытку переподключения; количество попыток, интервал
     и наблюдаемый признак попытки требованиями не заданы — пробел G-14 |
```

Ячейка утверждает факт и в той же строке признаёт, что наблюдаемого признака нет — проверить
шаг невозможно. Почему линтер прошёл мимо закономерно:

| Проверка линтера | Почему не сработала |
|---|---|
| Список запрещённых слов | «успешно», «корректно» в ячейке отсутствуют |
| Порог короткой ячейки (60 символов) | ячейка длинная — был бы MAJOR, не BLOCKER |
| Противоречие утверждения собственной оговорке | скриптом не выразимо в принципе |

**Блокер J02 — временная невозможность:**

> Шаг требует «Дождаться, пока таймлайн покажет 0:45», но к точке ветвления таймлайн заведомо
> больше 0:45 — ждать назад нельзя. Состояние не создаёт ни один шаг и ни одно предусловие.

Это находка прохода по цепочке состояний: нужно рассуждение о времени и унаследованных
предусловиях. Детерминированной проверкой не выражается.

**Вывод:** слоистая схема работает, и слои не заменяют друг друга. Скрипт снимает структуру за
миллисекунды, модель — смысл за минуты.

### 4.3. O-04 — база: порча JSON не случилась

Все три `output/state/<J>.json` проверены на схему: 10 полей, типы верные, `blockers` целое,
списки — списки. Сравнение `blockers == 0` отработало корректно во всех трёх случаях.

**На Claude порчи не произошло.** Значит O-04 обоснован не наблюдаемой частотой отказа, а его
ценой: любой из трёх вариантов порчи (строка вместо числа, пропущенное поле, лишняя
вложенность) молча сжигает три итерации — см. 1.2.

Для GigaCode поправка в **обратную** сторону: DeepSeek/Qwen ошибаются на вложенном JSON чаще,
здесь измерена благоприятная граница.

### 4.4. O-06 — ПОПРАВКА: я измерил не то

В разделе 3.2 записано «цена барьера — 37 мин 33 с». **Это простой ресурса, а не время прогона.**
Полный расчёт по обеим волнам:

| Journey | Дизайнер | Критик | Цепочка | Вердикт |
|---|---|---|---|---|
| J03 | 16:37 | 10:04 | 26:42 | FIX |
| J01 | 43:36 | 7:43 | 51:20 | PASS |
| J02 | 48:54 | 11:51 | **60:45** | FIX |

```
ВОЛНА (lockstep):  max(диз) + max(крит) = 48:54 + 11:51 = 60:45
КОНВЕЙЕР:          max(диз_j + крит_j)                  = 60:45
разница на итерации 1:                                     0:00
```

**На первой итерации барьер не стоил ничего** — потому что J02 оказался самым медленным
и среди дизайнеров, и среди критиков. Если бы узкое место в двух стадиях приходилось на разные
journey, разница появилась бы уже здесь.

**Где выигрыш реальный — на следующей итерации:**

| | Старт итерации 2 |
|---|---|
| Lockstep — волна 2 после всей волны 1 | 60:45 для обоих |
| Конвейер — чинится, как только вернулся свой критик | J03 в **26:42**, J02 в 60:45 |

**Фора J03 на итерации 2: 34 минуты 03 секунды.**

Условия, при которых барьер стоит времени:

1. **Итераций больше одной** — здесь именно так: 2 journey из 3 уходят на второй проход
2. Узкое место в стадиях приходится на разные journey
3. Journey больше, чем `P` — очередь пополняется только после всей волны

**O-06 остаётся приоритетным, но обоснование другое:** не «экономит N минут на итерации»,
а «даёт быстрым journey уйти вперёд, вместо того чтобы равняться на самый медленный».

### 4.5. Итог прогона

| | |
|---|---|
| Полное время (по расчёту волн) | **60 мин 45 с** на итерацию 1 |
| Токенов | **657 375** (аналитик 84 740 + дизайнеры 304 129 + критики 268 506) |
| Кейсов | 19 Markdown + 19 JSON |
| Линтер | 0/0/0 по всем |
| Критики | 2 BLOCKER, 13 MAJOR, 22 MINOR |
| Вердикты | 1 PASS, 2 FIX_REQUIRED |
| Пробелов найдено | 20 аналитиком + 19 дизайнерами |

---

## 5. Требует прогона на GigaCode — не измерено

| # | Что | Переносимость |
|---|---|---|
| O-00 | Параллельность реальная или номинальная | ❌ только на вашей инфраструктуре |
| O-01 | Доля печати против доли рассуждения у аналитика | ❌ зависит от модели |
| O-02 | Доля окна контекста | ❌ другой токенизатор |
| — | Сколько обращений к файлам делает аналитик фактически | ⚠️ частично |
| — | Итераций до PASS на journey | ⚠️ частично |
| — | Валиден ли JSON, который пишут агенты | ⚠️ частично |

Первые три должны быть измерены **на GigaCode**, иначе ответ будет про Claude и приведёт к
неверному решению по Этапу 2.

---

## 6. ПОСЛЕ — внесённые правки и замер

### 6.1. Сводка

| Измерение | До | После | Статус |
|---|---|---|---|
| Ложных блокеров линтера на сравнениях | **5 из 5** | **0 из 5** | ✅ O-05 |
| Настоящие заглушки ловятся | 2 из 2 | 2 из 2 | ✅ регрессии нет |
| Порча `state.json` перехвачена | **0 из 5** | **5 из 5** | ✅ O-04 |
| Индекс требований извлекается скриптом | 0 из 17 | **14 из 17** | ✅ O-03 |
| Барьер дизайнер→критик | lockstep | конвейер | ✅ O-06 |
| `max_turns` аналитика | 25 | 40 | ✅ O-13 |
| Эталонный кейс | 0/0/0 | 0/0/0 | ✅ регрессии нет |
| 19 кейсов прогона | 0/0/0 | 0/0/0 | ✅ регрессии нет |
| Реальные `state` (3 шт) + индекс | валидны | валидны | ✅ регрессии нет |

### 6.2. O-05 — `scripts/validate_cases.py`

Голый `"<"` убран из `PLACEHOLDERS`, вместо него — парная конструкция:

```python
PLACEHOLDER_SLOT = re.compile(r"<[^<>\s][^<>]{0,58}>")
```

Проверено на 12 формулировках, включая двойное сравнение «интервал 5 < t < 10 секунд»:
**12/12 верно, ноль ложных срабатываний.** `<название плейлиста>` и `<track_title>` по-прежнему
блокируются.

### 6.3. O-04 — `scripts/check_state.py` (новый)

Ловит все пять вариантов порчи, которые раньше проходили молча:

| Порча | До | После |
|---|---|---|
| `"blockers": "0"` строкой | проходила | **поймана** |
| поле пропущено | проходила | **поймана** |
| лишний уровень вложенности | проходила | **поймана** |
| `blockers: 3` при `verdict: PASS` | проходила | **поймана** |
| `blockers: false` (bool вместо int) | проходила | **поймана** |

Плюс проверка индекса аналитика: повторный номер `J<NN>` (источник N-02) и планы, которых нет
по указанному пути.

Пути к ревью резолвятся от расположения самого файла состояния, а не от текущего каталога —
иначе проверка ломается при запуске не из корня.

**Вызов встроен в `run.md`** шагом 3 цепочки: ненулевой код → `FAILED`, вердикт из непрошедшего
проверку файла не читается вовсе.

### 6.4. O-03 — `scripts/extract_reqs.py` (новый)

Разбирает оба формата корпуса: `- **REQ-01.** текст` и `| **BR-001** | … |`.

**Сверка с тем, что аналитик написал руками в этом прогоне:**

```
скрипт извлёк: 14        аналитик написал: 17
только у аналитика: REQ-15, REQ-16, REQ-17
только у скрипта:   нет
```

Расхождение ровно то, которое заложено в спецификацию: три недостающих — это ответы продукта
из `_answers.md`, у которых нет жирных идентификаторов, и их нумерация требует суждения.
**Механика — скрипту (82 %), суждение — аналитику (18 %).** Ни одного расхождения по
извлекаемой части.

Режим `--markdown --only` отдаёт готовый блок «Индекс требований» для вставки в план.

### 6.5. O-06 — конвейер вместо волн

`run.md` и `GIGACODE.md` переписаны: критик стартует, как только вернулся дизайнер **его**
journey, а не когда закончат все. Освободившийся слот сразу берёт следующий из очереди.

Обоснование в тексте — замеренное: разброс 2.94×, потерянная фора 34 минуты на итерацию.

### 6.6. O-13 — спецификация аналитика

- `max_turns` 25 → **40** (у дизайнера и писателей 40, у критика 30 — аналитик был единственным
  с 25 при самой большой задаче)
- Шаг 0 переписан: одно пакетное чтение, индекс скриптом, запрет возвращаться к исходникам
- Шаг 2: раздел «Индекс требований» вставляется выводом скрипта, а не печатается

### 6.7. Что осталось неизмеренным

Эффект O-03, O-06, O-13 на **время прогона** проверяется только повторным прогоном цепочки.
Детерминированно подтверждено, что скрипты работают и регрессии нет; сколько минут это снимет —
покажет повтор.

Ожидаемое по расчёту:
- O-06: фора 34 мин быстрому journey на итерации 2
- O-03: минус разбор прозы на Шаге 0, минус ~15 % печати
- O-13: минус round trip'ы (на Claude эффект меньше — он группирует вызовы сам)

---

*Раздел 6 добавлен 29.07.2026 после внесения правок O-03, O-04, O-05, O-06, O-13.*

---

## 7. Повторный прогон — замер «после» на живой цепочке

**Выполнен 29.07.2026, 15:28:03 → 16:07:38.** Тот же корпус, те же 3 journey, конвейер вместо волн.

### 7.1. Сводная таблица

| Стадия | Прогон 1 | Прогон 2 | Δ |
|---|---|---|---|
| Аналитик | 12:15 | **11:07** | −9 % |
| Дизайнеры (сумма) | 109:08 | **44:38** | **−59 %** |
| Дизайнеры (максимум) | 48:54 | **15:32** | −68 % |
| Критики (сумма) | 29:38 | **25:18** | −15 % |
| **Полный прогон** | **73:01** | **36:23** | **−50 %** |
| Токенов | 657 375 | 660 219 | +0 % |
| Кейсов | 19 | 15 | — |
| Линтер | 0/0/0 | 0/0/0 | — |
| Вердикты | 1 PASS, 2 FIX | 1 PASS, 2 FIX | — |

**Время пополам при тех же токенах.** Но приписывать это правкам целиком нельзя — см. 7.3.

### 7.2. Механики сработали

| Правка | Подтверждение из прогона |
|---|---|
| O-13 пакетное чтение | аналитик: **1 отдельное чтение** вместо 7, остальные 6 файлов одним пакетом |
| O-03 экстрактор | `_reqindex.json` создан на первой минуте; индекс в планах вставлен выводом скрипта |
| O-04 валидатор | все три критика вызвали `check_state.py`, код 0; постфактум все 4 файла валидны |
| O-05 линтер | 15 кейсов, 0 ложных блокеров |
| O-06 конвейер | критик J02 стартовал на 13:44, когда J01 и J03 ещё писали кейсы |

### 7.3. Чему приписывать выигрыш — честно

**Главную часть дала не оптимизация, а перегруппировка journey аналитиком.**

Структура изменилась с 7/11/8 этапов на 7/7/7. Работа дизайнеров выровнялась, и самый
показательный факт: **J03, чей объём почти не изменился, ускорился всего на 8 %** — 16:37 → 15:22.
Это и есть лучшая оценка вклада правок в скорость дизайнера. Остальные −64 % и −72 % у J01/J02
— эффект того, что им досталось меньше работы.

Критики ускорились на 15 % при сопоставимом объёме — здесь вклад правок виден чище: им дана
инструкция читать пакетом, и они её выполнили.

**Вывод: −50 % по прогону складывается из ~10-15 % от правок и остального от более ровной
декомпозиции.** Стабильность этого выигрыша не гарантирована — она зависит от того, как
аналитик сгруппирует journey в следующий раз.

### 7.4. O-06 — парадокс, который стоит записать

Фактический расчёт по прогону 2:

```
волна:    max(диз) + max(крит) = 15:32 + 9:53 = 25:26
конвейер: max(диз_j + крит_j)                 = 25:15
экономия конвейера:                              0:10
```

**Десять секунд.** В прогоне 1 при разбросе 2.94× потенциальная фора была 34 минуты; здесь
разброс упал до **1.13×**, и конвейеру нечего экономить.

**Парадокс: хорошая декомпозиция journey обесценивает конвейер.** Барьер дорог ровно тогда,
когда работа неравномерна. Чем ровнее аналитик режет journey, тем меньше выигрыш от снятия
барьера.

**O-06 при этом остаётся правильным решением** — как страховка, а не как источник экономии:

- разброс не контролируется: он зависит от того, как лягут требования на следующем корпусе
- при `MAX_ITERATIONS = 3` неравномерность накапливается — на итерации 2 остаются не все journey
- при journey больше `P` очередь при барьере пополняется только волнами

Стоимость конвейера нулевая, выигрыш от нуля до десятков минут. Но обещать по нему конкретную
экономию нельзя — что и показал этот прогон.

### 7.5. Качество не деградировало

| | Прогон 1 | Прогон 2 |
|---|---|---|
| BLOCKER найдено критиками | 2 | 3 |
| MAJOR | 13 | 17 |
| MINOR | 22 | 16 |
| Линтер по всем кейсам | 0/0/0 | 0/0/0 |
| `state.json` валидны | 3/3 | 3/3 |

Критики стали находить **больше** содержательных дефектов при меньшем числе кейсов (15 против 19).

Три блокера прогона 2 — того же класса, что скриптом не выразимы:

1. **J03** — шаг утверждает «сообщение о проблеме соединения снято с экрана», хотя REQ-13
   определяет только начало реакции; снятие сообщения не определено нигде, и собственный
   пробел кейса это признаёт
2. **J03** — «дождаться истечения интервала» проверяет часы, а не систему: шаг не может ни
   пройти, ни упасть
3. **J02** — шаг фиксирует «счётчик показывает 1» как pass/fail, при том что тот же кейс в
   пробеле G-10 объявляет ожидание неоднозначным, а вопрос Q-13 помечен блокирующим этот шаг.
   Кейс ответил на собственный незакрытый вопрос

**Новое наблюдение: критик ловит и выдуманные пробелы.** Критик J01 проверил источник и
опроверг пробел G-03 дизайнера: `_answers.md § 3` прямо озаглавлен «Q-03: Достижение лимита
пропусков **(Free)**» — круг пользователей назван. Ложный пробел так же вреден, как ложное
утверждение: он порождает вопрос человеку, на который ответ уже есть.

### 7.6. Итог

| Правка | Подтверждено | Эффект |
|---|---|---|
| O-05 | ✅ детерминированно | 5 ложных блокеров → 0 |
| O-04 | ✅ детерминированно + в потоке | 0 из 5 порч → 5 из 5 |
| O-03 | ✅ в потоке | 14 из 17 требований извлекаются механически |
| O-13 | ✅ в потоке | 7 отдельных чтений → 1 |
| O-06 | ⚠️ работает, но экономия 0:10 | зависит от разброса, который упал до 1.13× |

**−50 % времени при тех же токенах и без деградации качества** — но с оговоркой 7.3: бóльшая
часть выигрыша пришла от того, как аналитик сгруппировал journey, а не от правок.

---

## 8. Прогон 3 — малый корпус, `--no-ask`, первая проверка непроверенных фаз

**Дата:** 18.08.2026, коммит `7d08ed4` (ветка `skills`), run id `20260818T114515-b48ab7`.
**Цель:** не скорость, а исполнение поверхности, которую прогоны 1–2 не проходили: `start_run`,
шлюз требований в режиме `--no-ask`, `pending.md`, `--expect-iteration`, валидация агрегата,
суженное ревью на фикс-итерации, Фазы 2.5–4.
**Корпус:** только `zvuk-sample.md` (3.8 КБ, 14 REQ) + `_answers.md`; `zvuk.md` исключён.

### Хронометраж

| Стадия | Длительность | Результат |
|---|---|---|
| Фаза 1 — аналитик | 7:42 | **1 journey**, 12 этапов, 8 вариантов, 6 областей; REQ-15…17 донумерованы из `_answers.md` |
| Фаза 1.5 — шлюз `--no-ask` | ~1 мин | payload → defaults (P=1) → 27 строк в `pending.md`; `gate_check` пропущен по правилу |
| Фаза 2 — дизайнер iter1 | 27:27 | 18 файлов, основной кейс 36 шагов, линтер 0/0/0 с первого прохода |
| Фаза 2 — критик iter1 | 8:25 | FIX_REQUIRED: 1 BLOCKER, 1 MAJOR, 3 MINOR |
| Фаза 2 — дизайнер iter2 | 5:06 | 4 кейса правлены, 5 не тронуты, линтер чист |
| Фаза 2 — критик iter2 | 4:39 | **PASS 0/0/0**, все 5 находок iter1 закрыты |
| Фазы 2.5–4 | ~1 мин | умолчания «нет» / «как есть» → `pending.md`; `report.md` 105 строк |
| **Итого** | **57:24** | PASS за 2 итерации, 9 кейсов, 71 шаг, 17 открытых вопросов |

### Что подтвердилось исполнением

- **Блокер — целевого типа.** Единственный BLOCKER iter1 — выдуманное «активное состояние»
  индикатора лайка (TC-J01-00 шаг 19) при уже проверенных настоящих наблюдаемых REQ-07.
  Ровно тот класс дефекта, ради которого фабрика строилась; закрыт паттерном фиксации
  фактического поведения + G-16/Q-16.
- **`review_scope` на фикс-итерации: 9/9 re-review, перенос 0 — и это правильно.** Правка
  основного кейса добавила предусловие-фикстуру (трек ≥30 мин), которое варианты наследуют.
  Fail-closed сработал по делу, а не перестраховался. Экономия O-08 снова 0 % — третий прогон
  подряд блокер ложится в основной кейс.
- **`--expect-iteration` и валидация агрегата** прошли по разу на каждой итерации, находок нет —
  критики вернули согласованные числа.
- **`pending.md` накопил все три фазы** (1.5, 2.5, 3) и целиком вошёл в отчёт — механика
  «дописать, не перезаписать» работает.

### Что вскрылось

1. **У В1 не было default'а в контракте** — `defaults` payload'а несёт только `parallel`/`unit`;
   `coverage=continue` пришлось выводить из логики фазы. Закрыто в этом же коммите.
2. **Обратная сторона группировки.** Малый корпус → аналитик собрал всё в 1 journey → P=1,
   конвейер простаивает, дизайнер тянет 9 кейсов 27:27 (против ~15 мин на 5 кейсов в прогоне 2).
   Разрез 1×9 хуже разреза 3×3 и для параллельности, и для длительности одного агента —
   продолжение вывода 7.3 с другого конца.
3. **Рубрика и линтер тянут в разные стороны на границе формулировок:** дословная формулировка
   исправления, предложенная критиком, содержала «выполнено» — слово, которое линтер сам бы
   заблокировал. Дизайнер заметил и сохранил суть без слова. Пока наблюдение, не дефект.

### Что по-прежнему не исполнено

Фаза 2.5 с генерацией (`--generate-pytest`): `sync_markers` в цепочке, писатели, `--collect-only`
по всем journey. Проверены только по отдельности.

---

## 9. Прогоны 4–5 — корпус Apache Knox: цена размера корпуса, измеренная напрямую

> **Оговорка, добавленная после прогона 6 (§ 10):** цифры ниже — однопрогонные. Замеренный
> разброс между прогонами на идентичном входе составил +126 %, что больше приведённых здесь
> дельт. Направление выводов в силе, конкретные проценты читать как порядок величины.

**Дата:** 18.08.2026, ветка `skills`. Оба прогона: `--no-ask --generate-pytest`, включая
впервые исполненную цепочкой Фазу 2.5 (sync_markers → stub-writer → фактический прогон
pytest → сквозной collect).

**Прогон 4** (`20260818T142020-c7a86d`, коммит `b33f418`): корпус 10 REQ в двух файлах.
**Прогон 5** (`20260818T150306-ee096d`, коммит `011f963`): корпус 6 REQ в одном файле,
после целевого упрощения — варианты 4→1, аудит убран, оба противоречия исправлены в источнике.

| Стадия | Прогон 4 | Прогон 5 | Δ |
|---|---|---|---|
| Аналитик | 5:34 | 2:58 | −47 % |
| Дизайнер | 12:15 | 5:13 | −57 % |
| Критик | 5:45 | 3:46 | −34 % |
| Писатель (+маркеры) | 10:13 | 4:40 | −54 % |
| **Итого** | **37:32** | **19:18** | **−49 %** |
| Кейсов / шагов / тестов | 5 / 22 / 18 | 2 / 11 / 11 | |
| Вердикт | PASS iter1, 0/0/4 | PASS iter1, 0/0/1 | |
| Тесты фактическим pytest | 16 PASS / 2 SKIP | 11 PASS / 0 SKIP | |

### Выводы

1. **Время прогона ≈ линейно объёму артефактов.** Половина корпуса — половина времени, на
   каждой стадии по отдельности. Это прямое измерение того, что в § 7.3 и § 8 было выводом
   из косвенных данных.
2. **Противоречия в требованиях — это не только вопросы человеку, это токены во всех трёх
   агентах.** В прогоне 4 аналитик нашёл два реальных противоречия в корпусе (200↔201,
   401↔404) — они честно прокачались через план (7 пробелов), кейсы (TC-J01-04 заблокирован)
   и ревью. В прогоне 5 они исправлены в источнике: 3 пробела, 1 блокирующий вопрос, ни
   одного заблокированного кейса. Чистка требований — самая дешёвая оптимизация прогона.
3. **Фабрика ловит дефекты в обе стороны.** Противоречия из п. 2 были не подсадными —
   аналитик нашёл их в корпусе, который писал я и считал чистым. Двухсторонняя проверка
   (требования↔кейсы) работает.
4. **Ложный плейсхолдер `n/a` стоил прогону 4 реальных усилий**: подстрока матчилась внутри
   `admi·n/a·pi`, дизайнер прятал URL в код-спаны. Исправлено границами слова (коммит
   `011f963`); в прогоне 5 дизайнер прошёл те же URL без обходов, линтер чист на 20 кейсах
   регрессии. Класс O-05, второй экземпляр.
5. **Пол времени ≈ 15 минут при четырёх агентах** — фикс-минимум на чтение спек и написание
   отчётов не сжимается корпусом. Быстрее — только убрав агента: без `--generate-pytest`
   прогон 5 закончился бы на 14:18.

Эволюция полных прогонов: 57:24 (Звук, 14 REQ) → 37:32 (Knox, 10 REQ) → 19:18 (Knox, 6 REQ) —
все три PASS, каждый шаг выигрыша объясним измеренной причиной.

---

## 10. Прогон 6 — повтор на идентичном входе. Разброс больше измеряемого эффекта

**Дата:** 18.08.2026, ветка `mcp`, run id `20260818T170109-fc28c2`. Вход байт в байт тот же,
что в прогоне 5: `input/requirements-knox/knox-demo.md`, флаги `--no-ask --generate-pytest`.
Цель — проверить цепочку после замены `sync_markers` на регистрацию маркеров в корневом
`conftest.py` и **впервые замерить разброс между прогонами**.

| Стадия | Прогон 5 | Прогон 6 | Разница |
|---|---|---|---|
| Аналитик | 2:58 | 5:07 | +72 % |
| Дизайнер | 5:13 | ~9:24 (сбой + докат) | +80 % |
| Критик | 3:46 | 7:26 | +97 % |
| Писатель | 4:40 | 12:28 | +167 % |
| **Итого** | **19:18** | **43:37** | **+126 %** |
| Этапов / пробелов / блокирующих вопросов | 5 / 3 / 1 | 6 / 7 / 6 | |
| Кейсов / шагов / тестов | 2 / 11 / 11 | 2 / 16 / 16 | |
| Вердикт | PASS iter1, 0/0/1 | PASS iter1, 0/0/5 | |

### Это меняет чтение § 9 — и остальных замеров тоже

**Разброс на идентичном входе (+126 %) больше эффекта, который § 9 приписывал двукратному
сокращению корпуса (−49 %).** Причина не в случайном шуме: прогон 6 систематически дотошнее —
вдвое больше найденных пробелов, вшестеро больше блокирующих вопросов, впятеро больше MINOR,
на пять шагов длиннее основной кейс. Больше найденного — больше выходных токенов — больше
времени. Строгость агента гуляет в пределах, перекрывающих измеряемый эффект.

Что остаётся в силе и что нет:

- **Направление «меньше артефактов → меньше времени» неизбежно** — оно следует из того, что
  стена времени это выходные токены, и подтверждается внутри каждого прогона.
- **Конкретные проценты § 9 (−49 %, −57 % у дизайнера) — оценка порядка величины, не
  измерение.** Они получены n=1 против n=1 и включают неизвестную долю разброса.
- **Дельта критика −34 % между прогонами 4 и 5 больше не считается результатом** — она внутри
  разброса.
- **Все замеры § 1–9 однопрогонные.** Их выводы качественные, а не количественные; ни один
  не повторялся.

Чтобы мерить эффект правки честно, нужен n≥3 на стадию — а это часы. Практический вывод:
оптимизации выбирать по механизму (что именно перестанет генерироваться), а не по замеру
одного прогона.

### Побочные находки

1. **Корпус демо дырявый, и поймал это один аналитик из двух.** Требования формулируют URL
   через `<имя>` и `<путь к файлу>`, поэтому REQ-05 требует сверки «байт в байт» с эталоном,
   которого корпус не задаёт. Аналитик прогона 6 объявил это пробелом (G-04), прогон 5 прошёл
   мимо. Расхождение в строгости — не только про время.
2. **Восстановление после сбоя работает, и это следствие контракта директорий.** Дизайнер
   умер от ошибки API сразу после записи основного кейса; артефакт остался на диске, и
   продолжатель дописал JSON и вариант, не переделывая сделанное. Специально не проверяли.
3. **Третий ложный блокер линтера того же класса** — `data-unused` сверял целый токен в
   бэктиках, и хост `` `https://host:8443` ``, склеенный в шаге с путём, объявлялся
   неиспользованным. Исправлено сравнением по нормализованному тексту; проверено в обе
   стороны (ложное срабатывание ушло, настоящее неиспользуемое значение ловится),
   регрессия на 18 кейсах трёх корпусов чиста. После `<` в арифметике и `n/a` внутри
   `admi·n/a·pi` это уже закономерность: **правила линтера надо проверять на текстах,
   которые они будут видеть, а не только на тех, ради которых написаны.**
4. **Замена `sync_markers` подтверждена в бою:** маркер `J01` зарегистрирован из имени
   каталога, сквозная сборка 16 тестов чиста, Фаза 2.5 стала на шаг короче.

## 11. Прогоны 7–9 — воспроизводимость спецификаций на одном корпусе

**Дата:** 19.08.2026, ветка `skills`. Один и тот же вход во всех трёх: `input/requirements-knox/knox-demo.md`, режим `--no-ask`. Прогон 7 — полный цикл с `--generate-pytest` (42:04); прогоны 8 и 9 — Фазы 1–2 без генерации тестов, поскольку она идёт после закрытия Фазы 2 и кейсы только читает.

Вопрос был не «сколько это занимает», а «получаем ли мы дважды одно и то же».

| | прогон 7 | прогон 8 | прогон 9 |
|---|---:|---:|---:|
| Фазы 1–2, время | 26:49 | 23:23 | 37:59 (2 итерации) |
| итераций до PASS | 1 | 1 | 2 |
| journey / этапов / областей | 1 / 8 / 4 | 1 / 8 / 4 | 1 / 8 / 4 |
| покрытие REQ-01…06 | полное | полное | полное |
| вариантов | 3 | 3 | 2 |
| кейсов / шагов всего | 4 / 25 | 4 / 18 | 3 / 18 |
| шаги основного кейса | 14 | 10 | 11 |
| пробелов в плане | 6 | 8 | 7 |
| добавил дизайнер сверх плана | 2 | 3 | 8 |
| BLOCKER / MAJOR / MINOR | 0 / 3 / 5 | 0 / 1 / 7 | 0 / 0 / 2 |
| вердикт | PASS | PASS | PASS |

### Что воспроизводится

**Скелет — трижды одинаковый.** Один journey, ровно 8 этапов, 4 области, полное покрытие шести требований, ни одного этапа без якоря REQ. Последовательность этапов совпадает по смыслу во всех трёх: снимок реестра → PUT топологии → подтверждение в окне 30 с → LISTSTATUS → CREATE → OPEN с побайтовой сверкой → DELETE → 404 на снятом URL. Якоря REQ по этапам расходятся в одной клетке из восьми.

Все три отказались резать journey надвое и назвали одну причину: вторая половина унаследовала бы развёрнутую топологию — межjourney-зависимость, запрещённая правилом 8.

**Время Фаз 1–2 предсказуемее, чем считалось по разделу 10:** 26:49 / 23:23 при сходимости за одну итерацию. Разброс ±126 % из прогона 6 включал генерацию тестов и вдвое разный объём кейсов; сама тройка «аналитик → дизайнер → критик» держится в пределах 15 %.

### Что не воспроизводится

**Набор вариантов — пересечение 2 из 3.** «Неверный пароль на WebHDFS» и «листинг под `guest`» есть везде; «неверный пароль на админском PUT» прогон 9 не написал вовсе. Прогоны 7 и 8 при этом дали *одинаковый набор с разной нумерацией*: `TC-J01-01` одного — это `TC-J01-02` другого. Сравнение по идентификаторам покажет расхождение там, где содержание идентично.

**Развёртка этапа в шаги — свободный параметр.** Восемь этапов дают 14, 10 и 11 шагов основного кейса. Плана это не нарушает: `MIN_MAIN_STEPS = 8` есть, потолка нет — та же асимметрия, что была у этапов до введения потолка 8.

**Глубина вычитывания пробелов — втрое.** Дизайнеры добавили сверх плана 2, 3 и 8 новых пробелов.

**Классификация «блокирующий / advisory» плавает.** Вопрос «что вернёт запрос без заголовка `Authorization`» в прогоне 7 блокирующий, в прогоне 9 — advisory. По всем трём совпал только адрес стенда. А именно классификация определяет, что человек увидит на шлюзе и в каком порядке.

**Кто задаёт фикстуру — тоже не зафиксировано.** Прогоны 7 и 8 назвали топологию `demo-e2e` в плане; прогон 9 оставил в плане плейсхолдер `<имя>` (6 вхождений), и значение придумал уже дизайнер — `tc-j01-webhdfs`. Для будущего веера вариантных писателей (O-15) это существеннее самого имени: план с плейсхолдером даёт каждому писателю придумать своё.

### Главное: одинаковый дефект получил три разные оценки

Утверждение `HTTP 200` для `GET …/admin/api/v1/topologies` — кода, которого корпус не задаёт нигде:

- **прогон 7** — дизайнер написал 200, критик оценил **MINOR #4** («вывод почти тавтологический, но не отслежен — MINOR, не BLOCKER»), вердикт `PASS`;
- **прогон 8** — дизайнер сам заметил дыру и написал «HTTP-код требованиями не задан и на шаге не проверяется». Дефекта нет;
- **прогон 9** — дизайнер написал 200, критик оценил **BLOCKER**, вердикт `FIX_REQUIRED` и вторая итерация.

`PASS` против `FIX_REQUIRED` оказался не свойством артефакта. Один и тот же текст в кейсе прошёл шлюз в одном прогоне и остановил конвейер в другом.

### Вывод

Воспроизводимость расслаивается по одной линии — **есть ли у агента внешний ограничитель**. Где есть текст требования и числовой потолок (этапы, области, покрытие, порядок), совпадение близко к полному. Где ограничителя нет (шагов на этап, сколько пробелов вычитать, что блокирующее, MINOR это или BLOCKER), разброс 1.5–3 раза. Показательно, что этапы стали стабильны ровно с введением потолка 8: прогоны 6 и 7 на том же корпусе давали 6 и 8.

### Побочные находки прогонов 8–9

1. **`review_scope.py` хешировал не тот корпус.** `GLOBAL_INPUTS` содержал зашитый `input/requirements`, а прогоны шли по `input/requirements-knox/`. Замер с контролем: правка knox-корпуса отпечаток не меняла, правка zvuk — меняла. Значит правка требований между итерациями не вызывала глобальной инвалидации, и находки переносились как подтверждённые, хотя поведение под ними переопределили — fail-open внутри механизма, существующего ради fail-closed. Нашёл критик прогона 9. Исправлено (`b255f73`): пути берутся из `requirementsSource` индекса аналитика, при отсутствии или порче индекса — откат на корпус по умолчанию, никогда в пустой список.
2. **Итерация исправления правит кейсы, но не план.** В прогоне 9 недоказуемый `HTTP 200` убран из трёх шагов кейса, а в плане строка «Коды ответа операций» его по-прежнему утверждает. Следующая сверка кейсов с планом вернёт этот BLOCKER обратно.
3. **Шесть из семи MINOR прогона 8 — один механический дефект:** сквозная нумерация пробелов из плана против локальной нумерации вопросов, отчего ссылки «вопрос N» разъехались. Линтер этого не проверяет, хотя проверка полностью детерминированная.
4. **Итерация исправления вдвое дешевле первичного написания:** 4:45 против 9:35 у того же дизайнера на том же journey.

---

## 12. Прогон 10 — первый прогон с первичным JSON

**Дата:** 19.08.2026, ветка `new`, после слияния работы четырёх человек. Корпус тот же, что в прогонах 7–9: `input/requirements-knox/knox-demo.md`, режим `--no-ask`, без генерации pytest. Отличие одно и принципиальное: **дизайнер пишет кейс сразу в `.json`**, Markdown порождается `scripts/json_to_md.py`. Во всех предыдущих прогонах направление было обратным.

**Итог: 34:00, `PASS` с первой итерации, BLOCKER 0 / MAJOR 2 / MINOR 7.**

| Фаза | Время |
|---|---:|
| 1. Аналитик | 6:18 |
| 1.5. Шлюз + восстановление корпуса | ~2:30 |
| 2. Дизайнер (JSON) | 11:46 |
| 2. Критик | 12:13 |

### Направление работает

Линтер прошёл **с первого раза, без единой правки после него**; `check_artifacts` — 8 файлов из 8. Сочинять структуру агенту не тяжелее, чем прозу.

Объём лёг в середину разброса прошлых прогонов, то есть смена формата сама по себе на него не повлияла:

| | 7 | 8 | 9 | **10** |
|---|---:|---:|---:|---:|
| этапов | 8 | 8 | 8 | **8** |
| вариантов | 3 | 3 | 2 | **3** |
| кейсов | 4 | 4 | 3 | **4** |
| шагов всего | 25 | 18 | 18 | **21** |
| основной кейс | 14 | 10 | 11 | **12** |

Четвёртый прогон подряд даёт ровно 8 этапов и 4 области. Потолок, введённый после прогона 6, держит нарезку намертво — до него тот же корпус давал 6 и 8.

### Что вскрылось из-за смены формата

Два агента, дизайнер и критик, независимо назвали по несколько следствий. Совпали в главном.

1. **`steps[].requirements` — поле, ради которого направление и выбрано, и его никто не читает.** Дизайнер заполнил его во всех четырёх кейсах; конвертер выбрасывает (колонки с якорями в Markdown нет), линтер не проверяет, совпадение с `requirements` кейса не сверяется. Дизайнер назвал это «актом веры», критик пришёл к тому же с другой стороны.
2. **Паритет `.md` ↔ `.json` проверяется частично.** Линтер сверяет `id`, число шагов и три колонки шага. Предусловия, таблица данных, постусловия, пробелы и вопросы не сверяются ничем — правка этих разделов без перегенерации проходит молча.
3. **Проверка «запускали ли конвертер» была разрушающей.** Чтобы убедиться, надо запустить, а запуск и есть проверяемое действие: он стирает улику. Критик смог ответить только косвенно — линтер отработал раньше, и размеры порождённых файлов совпали побайтово. Исправлено режимом `--check`: сравнивает, падает кодом 1, ничего не пишет.
4. **Латентный дефект конвертера.** `_field` для списков возвращал `fallback` в обеих ветках, так что непустой список рендерился бы как «—». Ни одно поле шапки сегодня не список, но `requirements` им является: первое же его появление там стёрло бы себя молча. Исправлено.
5. **Ограничения, невидимые в схеме.** `|` и перевод строки в любом поле шага рвут таблицу (конвертер не экранирует); XML нельзя положить в `testData[].value`, потому что линтер считает заглушкой любое `<…>`. Записаны в `docs/format.md` § 5; правильнее — экранировать в конвертере, тогда запрет не нужен.

### Побочное: корпус исчез при слиянии, и это заметил агент

`input/requirements-knox/knox-demo.md` отсутствовал на диске. Ветка `astrarium-optimized` **переместила** его в `input/requirements/knox-demo.md`; git записал это переименованием, поэтому старый путь не попал в список удалений при слиянии, и файл был снят как «копия не в том каталоге» — вместе с корпусом.

Аналитик не упал и не выдумал требования. Он обнаружил пустой путь, восстановил текст из git, дописал в отчёт первой строкой «подтвердите, что это тот корпус» — и только потом построил план. Правило «не выдумывай» сработало на входных данных, а не только на ожидаемых результатах.

### Оговорка

Один прогон. По разделу 11 известно, что разброс между прогонами на одном корпусе — полтора-три раза по объёму, поэтому «34:00» здесь означает порядок величины, а не характеристику направления. Что подтверждено надёжно — это возможность: агент пишет валидный JSON первичным артефактом и проходит линтер без правок.

---

## 13. Прогон 11 — проверка девяти доработок в живом конвейере

**Дата:** 19.08.2026, ветка `new`. Тот же корпус Knox, `--no-ask`, плюс `--generate-pytest`: писатель тестов ни разу не работал с первичным JSON, ссылками на данные и разделом «Трассируемость».

**Итог: 48:49, `PASS` с первой итерации, BLOCKER 0 / MAJOR 2 / MINOR 4. Тесты: 25 собрано, 22 passed, 3 skipped, `--strict-markers` чист.**

| Фаза | Время |
|---|---:|
| 1. Аналитик | 6:33 |
| 1.5. Шлюз | ~1:30 |
| 2. Дизайнер | 10:36 |
| 2. Критик | 12:00 |
| 2.5. Писатель тестов | 16:38 |

Фазы 1–2 заняли 31:30 против 34:00 прогона 10 — внутри разброса, измеренного в § 11, поэтому вывода о скорости отсюда делать нельзя.

### Доработки сработали, две — не так, как задумывались

**Ссылки `{Имя параметра}`** использованы во всех пяти кейсах, ни одного вклеенного значения. Дизайнер отметил то, чего не предполагалось: проверка использования по имени, а не подстрокой, **сняла соблазн подгонять текст шага под линтер**. Раньше, чтобы удовлетворить `data-unused`, значение приходилось вписывать в шаг дословно, даже когда шагу это не нужно.

**«Трассируемость» задумывалась как удобство для человека, а стала детектором.** MAJOR #2 существует ровно потому, что порождённая таблица напечатала `REQ-04 | 1, 2`, тогда как оба этих шага сами сообщают, что ничего не проверяют. Ложное заявление покрытия, которое линтер пропускает — правило «требование проверяется хотя бы одним шагом» выполнено формально. В рукописном Markdown такого расхождения не было бы видно вовсе.

**`steps[].requirements` получило третьего потребителя.** Писатель тестов положил якоря в `@allure.label("req", …)` на каждый `test_` и объяснил, почему Markdown тут не годится: там трассируемость **агрегирована** (требование → номера шагов), а генератору нужна обратная проекция — шаг → требование. То есть поле, которое сутки назад никто не читал, теперь используют линтер, критик и писатель, причём последнему нужна форма, которой в Markdown нет по построению.

**Ловушка с HTTP-кодами не сработала.** Пять блокирующих вопросов корпуса остались без ответа, и ни один не закрыт домыслом: четыре операции, для которых требования не называют код, кода не утверждают. Стаб пошёл дальше и возвращает `UNSPECIFIED_STATUS = None` там, где код не задан, — тест физически не может утвердить его случайно.

### Две находки в самом проекте, обе от агентов

1. **Спеки обещали файл, которого никто не строил.** `qa-designer.md` и `test-critic.md` оба называют входом `output/suites/<JOURNEY_ID>-context.json` и оба говорят «его строит оркестратор» — а в процедуре такого шага не было. `extract_journey_context.py` пришёл со слиянием ветки `snapshot` и четыре прогона провисел неподключённым; критик читал полный индекс, включая чужие journey. Нашёл критик прогона 11, назвав это «шероховатостью входа».
2. **`docs/format.md` § 5 противоречил сам себе** про `steps[].requirements`: новый абзац «поле проверяется, из него порождается Трассируемость» и следом старый «генератор всегда пишет туда `[]`, ни один потребитель не читает». Дизайнеру пришлось идти в код. Второй за день случай, когда новый абзац лёг рядом со старым вместо него.

### Чего просит писатель тестов

Пять пунктов, все про машиночитаемость, ни одного про содержание:

- `steps[].testData` — свободная проза («URL, Basic X / Y, тело: Z»), метод, адрес и тело он разбирал из текста;
- нет флага «ожидаемый результат требованиями не задан» — генерацию `skip` пришлось строить на текстовой эвристике по формулировкам;
- ожидаемый HTTP-код живёт только в прозе `expectedResult`;
- нумерация пробелов и вопросов локальна для кейса и не совпадает с планом сьюты;
- ни план, ни кейсы не описывают исходное содержимое реестра топологий — стабу пришлось выбрать пустой список.

### Оговорка

Один прогон. Он показывает, что доработки **работают и используются по назначению**, и что два из них дали эффект сверх задуманного. Он не показывает, что стало быстрее: 31:30 против 34:00 лежит внутри разброса, измеренного на идентичном входе.

---

## 14. Первые настоящие цифры — профиль лога прогона на GigaCode

**Источник:** 617 записей `openai-*.json` с рабочей машины, прогон 18.08.2026, 05:49–09:30 UTC (3 ч 41 мин). До этого все замеры были секундомерными: сколько шла фаза. Здесь впервые видно, **на что уходит вход и выход**.

### Долг «расход `max_turns` не мерен» закрыт

Ход = один запрос к модели; сессия агента опознана по тексту задания (второе пользовательское сообщение — первое у GigaCode служебное и одинаково во всех сессиях).

| Агент | Сессий | Ходов: максимум | Медиана | Был лимит | Доля лимита |
|---|---:|---:|---:|---:|---:|
| `qa-designer` | 15 | **38** | 16 | 40 | **95 %** |
| `test-critic` | 11 | **24** | 15 | 30 | **80 %** |
| `requirements-analyst` | 2 | 10 | 10 | 40 | 25 % |
| оркестратор (GigaCode CLI) | 2 | 77 | 77 | — | — |

Лимиты стояли вслепую, и два из них оказались почти связывающими. Дизайнеру не хватило бы одного варианта или одной фикс-итерации: упёршийся в лимит агент возвращается как обычный, просто с меньшим числом файлов — это ловит `check_artifacts.py`, но ценой доката. Подняты: `qa-designer` 40 → 60, `test-critic` 30 → 45. Аналитику 40 оставлено, он тратит 10.

### Вход дороже выхода в 34 раза, и больше всех тратит оркестратор

| | Вход | Выход | Кэш на входе | Выход/вход |
|---|---:|---:|---:|---:|
| **всего** | **30 594 579** | **890 932** | 52.7 % | 2.91 % |
| оркестратор | 12 306 735 | 88 497 | 36.9 % | **0.72 %** |
| `qa-designer` | 11 060 215 | 573 788 | 72.1 % | 5.19 % |
| `test-critic` | 5 838 824 | 146 639 | 56.6 % | 2.51 % |
| `requirements-analyst` | 456 878 | 60 562 | 32.8 % | 13.26 % |

**Оркестратор съел 40 % всего входа, произведя 10 % выхода.** Его кэш-попадание 36.9 % — худшее среди рабочих агентов, а контекст к концу прогона доходит до 190 тыс. символов и 167 сообщений. Это цена «одного длинного разговора»: каждый ход тащит всю историю.

Дизайнер — противоположный профиль: 72 % попаданий в кэш и 64 % всего выхода проекта. Он и есть тот, кто пишет.

Аналитик по отношению выход/вход эффективнее всех (13 %), но в абсолюте это 1.5 % входа — оптимизировать его бессмысленно.

### Куда уходит время

Медианный интервал между запросами — 21.5 с, но распределение с тяжёлым хвостом: 51 % интервалов короче 5 с, а один интервал в 58 минут составляет **26 % всего прогона**. Из пятнадцати самых долгих интервалов одиннадцать — оркестраторские, и все они приходятся на ходы с контекстом 90–123 сообщения.

Длительности самого запроса в логе нет: `duration_ms` отсутствует, а `response.created` — внутренний счётчик, не время. Поэтому разделить «модель думала» и «оркестрация ждала» по одной записи нельзя; интервал между запросами содержит и то, и другое.

### Два дефекта в инструментах, которые синтетика показать не могла

1. **Атрибуция сваливала всё в одного агента:** 617 из 617 в `qa-designer`. Классификация шла подстроками по всему тексту, и проверка `'test case'` стояла раньше проверок на имена агентов — а системный промпт GigaCode CLI содержит и «test case», и имена всех агентов, потому что перечисляет их. Классифицируем по первой строке промпта: `# QA Test Designer`, `# Test Critic`.
2. **Падение на записи с `response: null`** — неуспешный запрос пишет ошибку в `error`, и `.get('usage')` валился с `AttributeError`.

Оба нашлись на первом же запуске по настоящим данным. Синтетика, на которой инструмент проверялся накануне, была правильной формы и потому бесполезной для этих двух — очередное подтверждение правила про тексты, которые правило увидит.

---

## 15. Прогоны 12 и 13 — крупный корпус и проверка сквозных id

### Прогон 12 — Звук, три journey, первый крупный корпус

**Дата:** 20.08.2026. 26 требований против 6 у Knox. Фазы 1–2 заняли 1 ч 35 мин, Фаза 2.5 — около 1 ч 20 мин с учётом сбоя.

| journey | итераций | B/M/m | кейсов | тестов |
|---|---:|---|---:|---:|
| J01 | 2 | 0/4/4 | 9 | 52 |
| J02 | 1 | 0/3/7 | 7 | 42 |
| J03 | 1 | 0/2/7 | 7 | 41 |

23 кейса, 135 тестов, **131 passed / 4 skipped**, `--strict-markers` чист. Коллизии имён модулей проверены на растущем дереве: писатель J02 собирал его дважды — на 94 тестах и на 135, когда третий агент дописал свой каталог, — оба раза без `import file mismatch`.

**Впервые проверено после слияния пяти веток:** несколько journey, параллельность 3, фикс-итерация с суженным ревью, ответы человека как требования, восстановление после сбоя по процедуре.

**Ответы человека сработали как требования.** 11 ответов на шлюзе закрыли 10 блокирующих вопросов из 10; вариантов стало 7/6/6 вместо 5/5/5 — появились те, чей ожидаемый результат раньше был не определён. Один вопрос **открыли сами ответы**: ANS-18 объявил «Любимое» системным плейлистом, ANS-13 задал лимит в три плейлиста, и стало неясно, входит ли одно в другое.

**Сбой и докат.** Писатель тестов J01 умер посреди работы (компьютер ушёл в сон), успев записать один файл из пяти. `check_artifacts` назвал недостающие четыре, докат дописал ровно их, `data_j01.py` остался нетронутым — mtime прежний. В прогоне 6 то же самое я вычислял руками за 6:44; здесь инструмент ответил за миллисекунды.

**Три писателя независимо проверили чувствительность своих тестов** — временно портили стаб, убеждались, что падают ровно нужные тесты, откатывали правку и прогоняли начисто. Никто их об этом не просил.

**Все четыре пропуска — отказ выбрать исход за требование.** Ни один не про лень: BR-005 называет «переход к предыдущему треку», но исхода не задаёт, а оставшиеся наблюдаемые истинны при любом; ANS-09 задаёт блокировку «до истечения часа», но не точку отсчёта. Стабы поднимают `RequirementGap` — тест не может позеленеть, если кто-то снимет пропуск раньше ответа человека.

### Что прогон 12 вскрыл в самой фабрике

1. **`--answers` терял ответ молча.** Заголовок `## 13. G-01 (довесок к Q-04)` не подходил под `Q-NN`: разобралось 12 из 13, без слова в stderr. Защита «ни одного ответа» не срабатывала — двенадцать не ноль.
2. **Контекст journey нёс уже отвеченные вопросы.** Индекс пишется до шлюза, контекст собирается из индекса. Дизайнера спасло только то, что бриф отдельно велел читать `_answers.md`; критик написал, что следующий без такой подсказки выпишет ложный BLOCKER.
3. **Потолок шагов сработал дважды, и диагноз оказался не тот, что я думал.** Критик J01 счёл дефектом правило («откалибровано на API-корпусе»). Критик J03 **проверил это, сходив в код**, и опроверг: эталон Звука — тот же интерфейсный корпус, он дробнее и укладывается в 1.58, потому что его план объявляет **12 этапов**. Настоящая причина — недообъявленные этапы: этапы 5, 6, 7 плана несли по три-четыре действия.
4. **Два моих потолка противоречат друг другу.** `MAX_STAGES = 8` обоснован фразой «при наблюдаемых 2.9 шага/этап», `MAX_STEPS_PER_STAGE = 2` объявляет находкой всё выше 2.0. Оба написаны в один день. Потолок этапов подталкивает к упаковке, потолок шагов бьёт по результату.
5. **Расхождение план ↔ кейсы — дефект контракта.** Дизайнер завёл `TC-J01-08`, чтобы закрыть находку, и не тронул план: `output/suites/` вне его зоны записи. Критик: «контракт держит два несовместимых правила — таблица „Варианты“ объявлена источником истины по составу, а единственный агент фикс-итерации не имеет права писать в план». Две из восьми находок итерации 1 имели правку плана исходом. Следствие хуже самого расхождения: `check_artifacts` считает по таблице и девятый кейс не видит.

### Прогон 13 — Knox, проверка сквозных id

**Дата:** 21.08.2026, 52:05, `PASS` с первой итерации, BLOCKER 0 / MAJOR 2 / MINOR 4.

Проверялись четыре правки, сделанные после прогона 12.

**Форма `{"id": …, "text": …}` у пробелов и вопросов — использована полностью:** ноль строковых элементов в шести кейсах. Оба агента оценили её независимо.

Дизайнер нашёл применение, которого правка не предполагала:

> `G-06` и `Q-05` (среда стенда) фигурируют в пяти кейсах из шести. При позиционной нумерации это были бы «пробел 2» в одном файле, «пробел 4» в другом и «пробел 3» в третьем — один и тот же пробел под тремя разными номерами.

Критик добавил второе:

> Она сделала возможной саму сквозную проверку — таблицу «id → файлы → текст» удалось собрать **механически**.

То есть правка не только убрала класс дефектов, но и дала критику инструмент, которого не было.

**Дизайнер сам нашёл предел правила.** Он дважды выдал `Q-J01-2` разным вопросам в разных кейсах, поймал сверкой вручную и сообщил: `ref-dangling` резолвит ссылку **внутри одного файла**, и оба варианта резолвились честно. Критик проверил все 18 идентификаторов во всех шести файлах — коллизии в выпущенных файлах нет, дизайнер починил до сдачи. Осталась одна находка того же рода: `Q-04` в разных файлах несёт разный объём вопроса.

**Подсказка про одно действие на этап сработала на предотвращение.** Основной кейс — 8 шагов на 8 этапов, плотность **1.0** при потолке 2.0; вчера на Звуке те же 8 этапов давали 16 и 20. Оговорка: Knox вчетверо меньше Звука, и вклад подсказки от вклада корпуса этот прогон не отделяет.

**Контекст journey не выдумал ответов** при отсутствии файла ответов — строки «закрыто ответами» нет, скрипт не упал.

**Обе MAJOR — про заявленное, но не обеспеченное покрытие**, и обе найдены через порождённый раздел «Трассируемость»: критик сверял «проверяет ли шаг то, что называет», вместо того чтобы набирать карту заново.

### Оговорка

Прогон 13 не вполне сравним с 7–11 на том же корпусе: я добавил в промпт аналитика строку про одно действие на этап. Сравнимость обменяна на проверку того, предотвращает ли подсказка дефект, а не только обнаруживает.

---

---

*Разделы 6–7 добавлены 29.07.2026. Прогон 2 выполнен на коммите `e020a2e`.
Раздел 8 добавлен 18.08.2026, раздел 9 — после прогонов Knox, раздел 10 — после повтора
на идентичном входе, раздел 11 — 19.08.2026 после трёх прогонов на воспроизводимость,
раздел 12 — в тот же день, первый прогон с первичным JSON,
раздел 13 — проверка девяти доработок в живом конвейере,
раздел 14 — 20.08.2026, профиль лога с рабочей машины,
раздел 15 — 21.08.2026, крупный корпус и сквозные id.*

---

*Замеры выполнены на коммите `2b721f2`, до внесения оптимизаций.*
